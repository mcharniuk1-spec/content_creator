# Датасет

Копия собранных данных на GitHub — вторая опора рядом с сервером.

| Что | Что внутри |
|---|---|
| `radar.db` | база радара: аккаунты, рилсы, метрики, темы, оценки |
| `radar-0907.db` | срез базы на прогон 7 сентября |
| `snapshots/` | снимки прогонов радара |
| `2026-08-niche-research/` | августовское исследование: пул рилсов, профили, транскрипты, разметка кадров |

Чего здесь нет и почему: **кадры роликов** (194 МБ, 2 222 файла) в гит не кладутся —
они распухнут в истории навсегда и не сжимаются. Живут в двух местах:
`ssh m2vps`, `/opt/radar/data/frames`, и локально в `data-archive/`. Сырой кэш ответов
HikerAPI за август (18 МБ) — там же.

Источник правды по накоплению — база на сервере. Здесь копия на случай потери сервера.
