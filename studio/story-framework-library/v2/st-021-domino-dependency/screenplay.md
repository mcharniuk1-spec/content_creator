# ST-021 — Domino Dependency

Truth state: `ORIGINAL_LOCAL_PREVISUALIZATION / EXTERNAL_PROVIDER_NOT_RUN`

## Logline

A systems explainer must show how one missing handoff propagates despite the dependency is invisible in a list, because one missing step can disrupt the full chain; the story resolves when the chain stops safely at the gate.

## Story spine

- Setup: a tabletop studio
- Inciting change: The domino changes state before the objective is secure.
- Objective: show how one missing handoff propagates
- Obstacle: the dependency is invisible in a list
- Stakes: one missing step can disrupt the full chain
- Action: notice the domino → removes one domino, then restores a labeled gate → inspect the stopped chain
- Proof: stopped chain
- Limitation: The metaphor simplifies real branching systems.
- Payoff: the chain stops safely at the gate

## Second-by-second screenplay

| Time | Role | Narration | Visible action | Object state | Shot / movement | A/B-roll | New information |
|---|---|---|---|---|---|---|---|
| 0–1s | hook | The state changed before anyone was ready. | notices the domino | ENT-trigger changes: ENT-target:dependency map=absent; ENT-proof:stopped chain=hidden → ENT-target:dependency map=unoriented; ENT-proof:stopped chain=hidden. | macro / push_in | full / attention_reset | Open a precise question with a visible changed object. |
| 1–2s | orientation | The objective is to show how one missing handoff propagates. | locates the dependency map | ENT-target changes: ENT-target:dependency map=unoriented; ENT-proof:stopped chain=hidden → ENT-target:dependency map=present-unresolved; ENT-proof:stopped chain=hidden. | wide / pull_out | voice_over / establish_context | Reveal actor, target, and spatial relationship. |
| 2–3s | mechanism | The decisive mechanism is to remove one domino, then restores a labeled gate. | removes one domino, then restores a labeled gate | ENT-hero, ENT-target changes: ENT-target:dependency map=present-unresolved; ENT-proof:stopped chain=hidden → ENT-target:dependency map=transformed; ENT-proof:stopped chain=hidden. | medium / tilt_down | voice_over / demonstrate_process | Show the mechanism transforming the persistent target. |
| 3–4s | obstacle | But the dependency is invisible in a list. | sees the dependency map blocked | ENT-obstacle, ENT-target changes: ENT-target:dependency map=transformed; ENT-proof:stopped chain=hidden → ENT-target:dependency map=blocked; ENT-proof:stopped chain=hidden. | close / locked | voice_over / concretize_abstraction | Make resistance and consequence inspectable. |
| 4–5s | proof | Check the stopped chain, not the claim. | checks the stopped chain | ENT-proof, ENT-target changes: ENT-target:dependency map=blocked; ENT-proof:stopped chain=hidden → ENT-target:dependency map=changed-pending-review; ENT-proof:stopped chain=visible. | diagram / rack_focus | voice_over / prove_state | Bind the changed target to an inspectable proof object. |
| 5–6s | handback | The bounded result: the chain stops safely at the gate. | returns the resolved target to the viewer | ENT-hero, ENT-target changes: ENT-target:dependency map=changed-pending-review; ENT-proof:stopped chain=visible → ENT-target:dependency map=resolved-with-boundary; ENT-proof:stopped chain=visible. | medium / pull_out | return_cut / emotional_texture | Close the visual argument and hand it back. |

## Read test

Read only the six frames in order. The protagonist, objective, block, corrective action, proof, limitation, and payoff must remain inferable without this document.
