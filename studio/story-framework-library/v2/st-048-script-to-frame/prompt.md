# Production prompts — ST-048

External image/video/audio provider execution remains NOT RUN. These prompts are future, provider-neutral replacement instructions.

## Continuity lock

{
  "hero_design": "anonymous circle-headed geometric operator with consistent accent color",
  "scene": "a frame wall",
  "trigger_prop": "script strip",
  "proof_prop": "verb markers",
  "target_prop": "action frames",
  "palette": [
    "#0B211A",
    "#85D69A",
    "#F5E1A4",
    "#FF8169"
  ],
  "screen_direction": "progress moves left to right; resistance blocks the center",
  "wardrobe": "non-identifying solid-color workwear",
  "forbidden_changes": [
    "no real-person likeness or brand marks",
    "do not reverse screen direction before payoff",
    "do not change prop identity between frames"
  ]
}

## ST-048-F01 · 0–1s

Prompt: 9:16 editorial storyboard still using the `process_handback` scene grammar. Anonymous geometric a storyboard artist in a frame wall. The script strip changes before the objective is secure. Keep ENT-target `action frames` persistent and visibly transform it through `replaces each abstraction with a hero verb and prop`; show ENT-proof `verb markers` when declared. Strong mobile silhouette, no logos, no real person, no final small text.

Avoid: No recognizable person, no brand interface, no copyrighted character, no watermark, no illegible generated copy, no unsupported result claim.

Motion intent: push_in; push_in motivated by open a precise question with a visible changed object.

## ST-048-F02 · 1–2s

Prompt: 9:16 editorial storyboard still using the `process_handback` scene grammar. Anonymous geometric a storyboard artist in a frame wall. The hero and persistent action frames share one scene; objective: turn narration into visible actions. Keep ENT-target `action frames` persistent and visibly transform it through `replaces each abstraction with a hero verb and prop`; show ENT-proof `verb markers` when declared. Strong mobile silhouette, no logos, no real person, no final small text.

Avoid: No recognizable person, no brand interface, no copyrighted character, no watermark, no illegible generated copy, no unsupported result claim.

Motion intent: pan_right; pan_right motivated by reveal actor, target, and spatial relationship.

## ST-048-F03 · 2–3s

Prompt: 9:16 editorial storyboard still using the `process_handback` scene grammar. Anonymous geometric a storyboard artist in a frame wall. The hero attempts to replace each abstraction with a hero verb and prop; the action frames visibly responds. Keep ENT-target `action frames` persistent and visibly transform it through `replaces each abstraction with a hero verb and prop`; show ENT-proof `verb markers` when declared. Strong mobile silhouette, no logos, no real person, no final small text.

Avoid: No recognizable person, no brand interface, no copyrighted character, no watermark, no illegible generated copy, no unsupported result claim.

Motion intent: match_move; match_move motivated by show an attempted causal operation on the target.

## ST-048-F04 · 3–4s

Prompt: 9:16 editorial storyboard still using the `process_handback` scene grammar. Anonymous geometric a storyboard artist in a frame wall. The hero replaces each abstraction with a hero verb and prop; the same action frames changes state on screen. Keep ENT-target `action frames` persistent and visibly transform it through `replaces each abstraction with a hero verb and prop`; show ENT-proof `verb markers` when declared. Strong mobile silhouette, no logos, no real person, no final small text.

Avoid: No recognizable person, no brand interface, no copyrighted character, no watermark, no illegible generated copy, no unsupported result claim.

Motion intent: pan_right; pan_right motivated by show the mechanism transforming the persistent target.

## ST-048-F05 · 4–5s

Prompt: 9:16 editorial storyboard still using the `process_handback` scene grammar. Anonymous geometric a storyboard artist in a frame wall. The changed action frames remains visible while the verb markers is isolated. Keep ENT-target `action frames` persistent and visibly transform it through `replaces each abstraction with a hero verb and prop`; show ENT-proof `verb markers` when declared. Strong mobile silhouette, no logos, no real person, no final small text.

Avoid: No recognizable person, no brand interface, no copyrighted character, no watermark, no illegible generated copy, no unsupported result claim.

Motion intent: rack_focus; rack_focus motivated by bind the changed target to an inspectable proof object.

## ST-048-F06 · 5–6s

Prompt: 9:16 editorial storyboard still using the `process_handback` scene grammar. Anonymous geometric a storyboard artist in a frame wall. The resolved action frames stays visible while the sequence returns to A-roll: every frame performs part of the sentence. Keep ENT-target `action frames` persistent and visibly transform it through `replaces each abstraction with a hero verb and prop`; show ENT-proof `verb markers` when declared. Strong mobile silhouette, no logos, no real person, no final small text.

Avoid: No recognizable person, no brand interface, no copyrighted character, no watermark, no illegible generated copy, no unsupported result claim.

Motion intent: pull_out; pull_out motivated by close the visual argument and hand it back.
