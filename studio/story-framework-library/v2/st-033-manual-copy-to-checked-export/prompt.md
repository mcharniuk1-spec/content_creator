# Production prompts — ST-033

External image/video/audio provider execution remains NOT RUN. These prompts are future, provider-neutral replacement instructions.

## Continuity lock

{
  "hero_design": "anonymous circle-headed geometric operator with consistent accent color",
  "scene": "a transfer bench",
  "trigger_prop": "clipboard",
  "proof_prop": "hash receipt",
  "target_prop": "versioned export",
  "palette": [
    "#0B211A",
    "#85D69A",
    "#F5E1A4",
    "#FF8169"
  ],
  "screen_direction": "progress moves left to right; resistance blocks the center",
  "wardrobe": "non-identifying solid-color workwear",
  "forbidden_changes": [
    "no real-person likeness or brand marks",
    "do not reverse screen direction before payoff",
    "do not change prop identity between frames"
  ]
}

## ST-033-F01 · 0–1s

Prompt: 9:16 editorial storyboard still using the `before_after_transformation` scene grammar. Anonymous geometric a data operator in a transfer bench. The clipboard changes before the objective is secure. Keep ENT-target `versioned export` persistent and visibly transform it through `exports stable IDs, nulls, and checksums`; show ENT-proof `hash receipt` when declared. Strong mobile silhouette, no logos, no real person, no final small text.

Avoid: No recognizable person, no brand interface, no copyrighted character, no watermark, no illegible generated copy, no unsupported result claim.

Motion intent: locked; locked motivated by open a precise question with a visible changed object.

## ST-033-F02 · 1–2s

Prompt: 9:16 editorial storyboard still using the `before_after_transformation` scene grammar. Anonymous geometric a data operator in a transfer bench. The hero and persistent versioned export share one scene; objective: move records without losing lineage. Keep ENT-target `versioned export` persistent and visibly transform it through `exports stable IDs, nulls, and checksums`; show ENT-proof `hash receipt` when declared. Strong mobile silhouette, no logos, no real person, no final small text.

Avoid: No recognizable person, no brand interface, no copyrighted character, no watermark, no illegible generated copy, no unsupported result claim.

Motion intent: locked; locked motivated by reveal actor, target, and spatial relationship.

## ST-033-F03 · 2–3s

Prompt: 9:16 editorial storyboard still using the `before_after_transformation` scene grammar. Anonymous geometric a data operator in a transfer bench. The versioned export is visibly blocked by manual copy strips IDs and null states; consequence: downstream analysis can misread absence. Keep ENT-target `versioned export` persistent and visibly transform it through `exports stable IDs, nulls, and checksums`; show ENT-proof `hash receipt` when declared. Strong mobile silhouette, no logos, no real person, no final small text.

Avoid: No recognizable person, no brand interface, no copyrighted character, no watermark, no illegible generated copy, no unsupported result claim.

Motion intent: push_in; push_in motivated by make resistance and consequence inspectable.

## ST-033-F04 · 3–4s

Prompt: 9:16 editorial storyboard still using the `before_after_transformation` scene grammar. Anonymous geometric a data operator in a transfer bench. The hero exports stable IDs, nulls, and checksums; the same versioned export changes state on screen. Keep ENT-target `versioned export` persistent and visibly transform it through `exports stable IDs, nulls, and checksums`; show ENT-proof `hash receipt` when declared. Strong mobile silhouette, no logos, no real person, no final small text.

Avoid: No recognizable person, no brand interface, no copyrighted character, no watermark, no illegible generated copy, no unsupported result claim.

Motion intent: pan_right; pan_right motivated by show the mechanism transforming the persistent target.

## ST-033-F05 · 4–5s

Prompt: 9:16 editorial storyboard still using the `before_after_transformation` scene grammar. Anonymous geometric a data operator in a transfer bench. The changed versioned export remains visible while the hash receipt is isolated. Keep ENT-target `versioned export` persistent and visibly transform it through `exports stable IDs, nulls, and checksums`; show ENT-proof `hash receipt` when declared. Strong mobile silhouette, no logos, no real person, no final small text.

Avoid: No recognizable person, no brand interface, no copyrighted character, no watermark, no illegible generated copy, no unsupported result claim.

Motion intent: rack_focus; rack_focus motivated by bind the changed target to an inspectable proof object.

## ST-033-F06 · 5–6s

Prompt: 9:16 editorial storyboard still using the `before_after_transformation` scene grammar. Anonymous geometric a data operator in a transfer bench. The same versioned export reaches its resolved state: the receiver can verify the file. Keep ENT-target `versioned export` persistent and visibly transform it through `exports stable IDs, nulls, and checksums`; show ENT-proof `hash receipt` when declared. Strong mobile silhouette, no logos, no real person, no final small text.

Avoid: No recognizable person, no brand interface, no copyrighted character, no watermark, no illegible generated copy, no unsupported result claim.

Motion intent: pull_out; pull_out motivated by close the opening question with the changed target.
