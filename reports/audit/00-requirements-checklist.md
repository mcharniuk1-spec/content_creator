# M2Radar Content Engine — Definition-of-Done Tracker

## Phase 1: Repository Merge & Current Corpus Analysis

| ID | Section | Requirement (one line, verbatim-close) | Type | Status | Evidence |
|----|---------|---------|------|--------|----------|
| R-001 | 2/49 | Do not delete `latest` branch | git | ✓ done | `origin/Latest` remote ref intact at `31ed2d41...`, never deleted (`git branch -a`; `.git/logs/refs/remotes/origin/Latest` shows only a fetch entry). |
| R-002 | 2/49 | Do not overwrite Max's fork history | git | ✓ done | `remotes/max/main` @ `6462f409` reachable and unrewritten; `reports/final/partA/03-main-vs-latest-comparison.md` confirms no history rewrite. |
| R-003 | 2/49 | Preserve all data; do not discard old cards, prior analytical outputs, transcripts, or frame data | git | ✓ done | `data/migration_log.md`: every table delta `+0`; `archive/`, `docs/archive-2026-09/`, `audit-2026-09-08/` retained on disk; `data/unmatched.md` keeps unresolved records rather than dropping them. |
| R-004 | 3 | Inspect main, `latest`, Max's fork, other branches, repository history, and migration history | doc | ✓ done | `reports/audit/01-branch-comparison.md` (811 lines) + `reports/final/partA/03-main-vs-latest-comparison.md` cover main/Latest/max/history/migration in commit-level detail. |
| R-005 | 3 | Inspect database models, analytics scripts, state tracing code, and frame extraction code | code | ✓ done | `docs/M2RADAR_CONTENT_ENGINE_ARCHITECTURE.md` §4-5, §9 + `reports/final/partA/02-repository-architecture-review.md`. |
| R-006 | 3 | Inspect cards, scripts, reports, generated artifacts, Notion exports, Hiker integration, Loore integration, local transcription, Remotion, OpenGenAI, previous agent prompts, knowledge-base files, and previous Luna outputs | data | ◐ partial | Cards/scripts/reports/Notion/Hiker/Loore/Remotion inspected with citations (`reports/audit/03,04,05`); "Luna" searched exhaustively and confirmed nonexistent (`reports/audit/04-existing-cards-review.md` §D). "OpenGenAI" never appears anywhere in the repo outside this checklist itself — flag as an unverifiable/nonexistent inspection target, not a missed inspection. |
| R-007 | 3 | Create explicit branch comparison answering: what exists only in main vs latest vs Max fork; what is newer/more reliable; which database is more complete; which schema has correct fields; which pipeline supports state tracing; which is canonical | doc | ✓ done | `reports/audit/01-branch-comparison.md` §1-5 + `reports/final/partA/03-main-vs-latest-comparison.md` answer every sub-question with an explicit canonical-per-layer table. |
| R-008 | 4 | Review all existing usable data programmatically/analytically (not sampling) | analysis | ✓ done | `reports/audit/02-data-inventory.md`: mirror stated "byte-identical to production"; full-corpus counts, not a sample. |
| R-009 | 4 | Explicitly report total creators, videos, stats, transcripts, frames, cards, scripts, Luna outputs, and unusable records | doc | ✓ done | `reports/audit/02-data-inventory.md` §1: creators 1,357, reels 5,147/3,211 unique, transcripts 273 (266 usable/7 dead), frames 2,653 rows, cards 23, deep dives 282; Luna outputs = 0 (confirmed nonexistent). |
| R-010 | 5 | Create schema reconciliation document mapping source/canonical for every entity group | doc | ✓ done | `docs/M2RADAR_DATABASE_RECONCILIATION.md` (451 lines): explicit per-entity source→canonical mapping table. |
| R-011 | 5.1-5.6 | Implement Creator/Profile entity with 20+ attributes (ID, platform, username, followers, posting frequency, consistency metrics, topic/hook/CTA distribution, positioning) | data | ✓ done | `accounts` table (19 cols) + `creator_stats` (31 cols, counted via `PRAGMA table_info`) ≥ 20 attributes incl. followers, posting frequency, consistency, topic/hook/CTA distributions, positioning. |
| R-012 | 5.1-5.6 | Implement Video/Reel entity with 15+ attributes (ID, creator, URL, caption, duration, views, likes, comments, shares, saves, engagement, metric timestamp) | data | ✓ done | `reels` table = exactly 15 columns counted directly (code/pk_user/username/ts/kind/play/likes/comm/resh/save/dur/cap/followers/fetch_id); engagement/rate fields live in `video_features`. |
| R-013 | 5.1-5.6 | Implement Transcript entity with 10+ attributes (ID, video ID, provider, language, full text, word/token/character/speech duration counts, version, timestamps) | data | ✓ done | `transcripts` (5 cols) + `transcript_meta` (17 cols) together cover provider/language/tokens/chars/speech_seconds/wps/version/timestamps — well over 10 attributes. |
| R-014 | 5.1-5.6 | Implement Transcript Segments with 20+ attributes (segment ID, transcript ID, video ID, time range, text, word count, semantic role, topic, subject, pain, hook, solution, proof, CTA, transition, emotional driver, content function) | data | ◐ partial | `beats` table = 21 columns counted (exceeds the numeric 20+ bar), but topic/subject/pain/hook/solution/proof/CTA/transition are collapsed into a single `role` enum rather than separate columns — numeric threshold met, literal field-by-field match is not. |
| R-015 | 5.1-5.6 | Implement Frames/Scenes with 20+ attributes (frame ID, video ID, timestamp, scene type, A-roll/B-roll indicator, speaker/face/product/UI/text presence, caption placement, dominant action, visual density, analytical labels, provider) | data | ◐ partial | `frame_labels` = 21 cols + `scenes` = 13 cols counted directly; speaker/face/UI/text-presence flags present, but no dedicated `product_present` column (would live inside `labels_json`). |
| R-016 | 5.1-5.6 | Implement Analytical Features with 60+ queryable fields (topics, pain, solution, hook type, transcript metrics, visual construction, performance baseline, relative performance) | data | ✓ done | `video_features` = 73 columns counted directly (`PRAGMA table_info`), comfortably over the 60+ bar: topic/pain/solution/hook/CTA, transcript metrics, visual construction, performance baseline and relative performance. |
| R-017 | 6 | Perform source schema inventory, column mapping, data type validation, and key mapping | data | ✓ done | `docs/M2RADAR_DATABASE_RECONCILIATION.md` §9 per-field validation rules; concrete edge cases named (e.g. 226 codes with NULL saves). |
| R-018 | 6 | Perform creator/reel/transcript/frame/card identity reconciliation and duplicate detection | data | ✓ done | `data/migration_log.md` "Integrity" table: 1 code with two creator ids, 25 duplicate caption groups, 56 duplicate caption codes — explicit duplicate detection performed. |
| R-019 | 6 | Validate row counts before/after migration, unique creator/video/transcript/frame counts, and check for orphan records and broken foreign keys | data | ✓ done | `data/migration_log.md`: every row-count delta `+0`; `integrity_check ok`; 0 FK violations; orphans itemized (6 transcripts/frames without a `reels` row). |
| R-020 | 6 | Store unresolved/unmatched records in reviewable migration log; do not silently drop unmatched records | doc | ✓ done | `data/unmatched.md` + `data/migration_log.md` "UNRESOLVED records — kept, not dropped, not invented" section names all 6 orphan codes with disposition. |
| R-021 | 7 | Implement state tracing system with 40-state lifecycle (DISCOVERED through COMPLETED/FAILED/RETRY_REQUIRED) | code | ◐ partial | `engine/state.py` `STAGES` = 25 states, not 40 (`reports/final/partA/06-state-tracing.md`: "trimmed from his 41-stage DAG ... to the 25 stages this branch actually runs"); `JOB_STATES` lifecycle (PENDING/RUNNING/DONE/FAILED/RETRY_REQUIRED/SKIPPED) implemented and unit-tested. |
| R-022 | 7 | Each trace captures job ID, parent job ID, entity IDs, state, execution time, duration, agent, provider, model, prompt version, knowledge version, input/output artifact IDs, data version, retries, errors, validation status, cost/token metadata | data | ✓ done | `engine/schema.py` `jobs` table carries every named field (job_id, parent_job_id, entity_kind/id, state, timestamps, agent, provider, model, prompt/knowledge version, input/output refs, data_version, retries, error, validation, cost_json); populated with 265 real rows and 7 `runs` (`python3 -m engine.schema --check`). |
| R-023 | 8/504 | Integrate working server-connected Hiker implementation into Git without exposing the API key | code | ✓ done | `lib/hiker.py` is in git; `reports/audit/03-server-runtime-hiker.md` confirms server code == git main exactly; repo-wide `grep` finds only env-var/header names, no literal key. |
| R-024 | 8/504 | Include Hiker configuration validator, state transitions, normalization logic, job orchestration hooks, tests, and documentation | code | ✓ done | Validator `engine/hiker_config.py::check()`; normalization `lib/hiker.py::row()`; job hooks via `run.py`'s `_job()`; tests `tests/test_hiker_client.py` (13) + `tests/test_providers.py` (25); docs in architecture doc §7. |
| R-025 | 8/504 | Remove hard-coded secrets; use environment variables with placeholder names in .env.example | code | ✓ done | `.env.example` lists only empty-valued placeholder names (`HIKER_KEY=` etc.); no hardcoded secret found anywhere in the repo. |
| R-026 | 8/504 | Normalize Hiker responses into canonical creator/video/stat models with raw payload provenance | code | ◐ partial | `lib/hiker.py::row()` normalizes into canonical fields; `fetch_log` table exists for raw-payload provenance (`engine/schema.py`) but holds 0 rows — no collection run has executed against this schema version yet. |
| R-027 | 8/504 | Ensure committed architecture can be deployed to server without re-implementing Hiker manually | code | ✓ done | Server code identical to git main at the same commit (`reports/audit/03-server-runtime-hiker.md` §1). |
| R-028 | 9 | Use local transcription as default/primary automated path; keep Loore/Lure as optional fallback only | code | ✓ done | `engine/local_pipeline.py` runs faster-whisper as the only transcription call site; Loore is never called from any pipeline code (`reports/final/partA/08-transcript-pipeline.md`). |
| R-029 | 9 | Implement local transcription requirements: preserve timestamps, segments, language metadata; calculate word/token/character counts, speech duration, words-per-second; save model/version; be idempotent and retryable | code | ✓ done | `engine/local_pipeline.py::process_video` TRANSCRIPTION step stores timestamps/segments/language, computes words/tokens/chars/speech_seconds/wps, versioned `asr_version`, idempotent+retryable; tested (`tests/test_local_pipeline.py`). |
| R-030 | 9 | Do not make automated runs depend on Loore/Lure; ensure missing credentials never block local pipeline; mark as OPTIONAL_PROVIDER | code | ✓ done | `engine/providers.py`: `loore` registered `OPTIONAL_PROVIDER`/DISABLED even with a key present unless `LOORE_ENABLED=1`; no call site invokes it (tested). |
| R-031 | 10/62 | Perform exhaustive 100% transcript review/analysis; analyze every valid essential transcript (not sampling, not only top performers) | analysis | ◐ partial | 266/268 usable transcripts analysed (99.25%), but the underlying 268-code corpus is itself only 8.3% of 3,211 ingested codes and is explicitly the top-ranked slice of `score.py`'s own ranking, not a random/complete set (`reports/final/partB/13-transcript-categorization.md`; execution report §14 item 1). |
| R-032 | 10/62 | Identify for each transcript: hook, setup, pain, explanation, solution, proof, payoff, CTA, and structural/emotional/persuasion roles | analysis | ✓ done | `beats.role` covers hook/setup/pain/explanation/solution/proof/payoff/cta plus structural/emotional/persuasion fields; 1,527 beats across 264 codes. |
| R-033 | 10/62 | Store for each transcript chapter: time range, word/sentence/phrase counts, percentage of total duration, semantic role, audience function, emotion, intention, persuasion role, overlapping scenes, cuts, key phrases | data | ✓ done | `beats` columns: start_s/end_s/duration_s, words/sentences, share_of_speech, role, audience_function, emotion, intention, persuasion, key_phrases_json, scene_ids_json; no separate per-beat "cuts" field. |
| R-034 | 11/683 | Estimate/calculate duration and word count for major script components (hook, setup, pain, solution, proof, CTA) | analysis | ✓ done | `reports/final/partB/19-script-part-lengths.md`: median/mean seconds+words per part (n=264). |
| R-035 | 11/683 | Analyze distributions: typical hook/problem/solution/CTA lengths; average/median part shares; differences by creator/topic/archetype | analysis | ✓ done | Same report: full distributions (p25/median/p75), strong-vs-weak comparison, breakdown by creator/topic/archetype. |
| R-036 | 11/683 | Analyze frequency of hook types, pain types, solution patterns, proof types, CTA types, narrative structures, transitions, topics, phrases, rhetorical devices | analysis | ✓ done | `reports/final/partB/20-script-frequency-patterns.md`: 146 distinct orderings, hook/CTA-type frequencies, 975 rhetorical-device strings (documented as non-countable free text). |
| R-037 | 12 | Create multi-agent analytical layer with conceptual roles: Structural Analyst, Marketing/Persuasion Analyst, Performance Analyst, Hook Specialist, Narrative Analyst, Audience/Positioning Analyst, Script Reviewer, Synthesis Agent | code | ◐ partial | `docs/M2RADAR_CONTENT_ENGINE_ARCHITECTURE.md` lines 394-401 map all 8 conceptual roles to concrete modules/prompts; only 4 actual prompt files exist (`engine/prompts/`) — several roles are folded into one ta-v1 prompt's structured output rather than separate agents. |
| R-038 | 13 | Review all existing scripts/cards: verify references were justified statistically, analyze quality of source material use, classify as strong/usable/weak/generic/unsupported/incomplete | analysis | ✓ done | `reports/audit/04-existing-cards-review.md`: all 33 known cards reviewed with per-card classification (Strong/Usable/Weak/Generic/Unsupported/Incomplete) matching the required vocabulary. |
| R-039 | 13 | Produce general conclusions: what worked, what did not, what should be preserved/changed, which prior cards are useful templates | doc | ✓ done | Same file: "Templates to reuse", "Templates that must not guide future generation", 4 named process defects, recommended field list for next format. |
| R-040 | 14/818 | Extract frames locally by default; detect scene boundaries, visual changes, meaningful keyframes; create timestamped scene intervals aligned to transcript | code | ◐ partial | `engine/scenes.py` implements real scene-boundary detection, tested; per `reports/final/partA/09-frame-pipeline.md` no code in the DB yet carries `frames_version='scene-v1'` — all existing frame data is the legacy fixed-9-sample method. |
| R-041 | 14/818 | Classify visual function: opening shot, A-roll, B-roll, split screen, screen recording, product shot, UI demo, talking head, close-up, medium/wide shot, text-only, subtitles, charts, memes, visual proof, overlay, CTA, transitions, motion graphics | data | ✓ done | `frame_labels.frame_type` taxonomy applied to 295 codes/2,645 frames (`reports/final/partB/21-frame-analysis.md`); sampled at 9 fixed points per video, not continuous coverage. |
| R-042 | 14/818 | Use hybrid local approach: scene-change detection, perceptual difference, transcript-segment boundaries, keyframe extraction, visual-change intensity, semantic grouping, OCR where reliable | code | ◐ partial | Scene-change detection, transcript-segment boundaries and keyframe extraction present; no OCR anywhere in the repo (grep-confirmed), no distinct "perceptual difference" method beyond the scene-score threshold, no evidenced semantic-grouping step. |
| R-043 | 14/818 | Make local frame processing idempotent, retryable, state-traced, versioned, safe to rerun when algorithm changes | code | ✓ done | `process_video()` idempotent + retryable + state-traced + versioned (tested: `test_process_video_is_idempotent`, `test_process_video_force_reruns`); not yet run in production against the full corpus. |
| R-044 | 15/902 | Review all existing frame records; validate timestamps/file references; associate with transcript segments; identify missing sequences, duplicates, low-quality samples; normalize metadata; enrich analytical labels | analysis | ✓ done | `reports/final/partA/09-frame-pipeline.md`: timestamps/files validated (2,645/2,653 present, 8 broken all on one code), duplicates found, 13 orphan sheets identified, alignment measured. |
| R-045 | 15/902 | Incorporate full frame corpus into final architecture; do not ignore frame data only in main or assume `latest` contains everything | data | ✓ done | August-archive import folded +74 rows into `frames` table; `engine.corpus.tiers()` gives FRAMES_ONLY its own tier (22-29 codes) rather than dropping frame-only data. |
| R-046 | 16/924 | Analyze for each video: first frame, hook frame, first 1-3-5 seconds, scene count, cut frequency, average scene length, A-roll/B-roll ratio, split-screen usage, text overlay, subtitle density, camera framing, face prominence, body movement, visual proof, visual metaphor, transition style, synchronization | analysis | ◐ partial | First-frame/hook-frame/scene-count/cut-frequency/roll-ratio/split-screen/text-overlay/framing/face-presence all covered (`reports/final/partB/21-24`); "body_movement" and "visual_metaphor" not found as named fields anywhere. |
| R-047 | 16/924 | Assess transcript-to-frame alignment: does visual illustrate claim, provide proof, create contrast, or merely decorate; does visual change at semantic boundaries | analysis | ✓ done | `reports/final/partB/25-transcript-to-frame-alignment.md` measures semantic-boundary cut rate (3.6%), explicitly labelled a measurement floor / INSUFFICIENT given the 9-sample grid's coarse resolution rather than a fabricated finding. |
| R-048 | 17/969 | Create controlled frame/scene taxonomy: A_ROLL_TALKING_HEAD, B_ROLL_CONTEXT, SCREEN_RECORDING, SCREENSHOT, SPLIT_SCREEN, TEXT_ONLY, DATA_VISUAL, MEME, PROOF_VISUAL, HOOK_VISUAL, CTA_VISUAL, TRANSITION, MOTION_GRAPHIC, OVERLAY | data | ✓ done | `engine/SPEC.md` lines 233-236 define the exact taxonomy (A_ROLL_TALKING_HEAD...OVERLAY) matching the requirement almost verbatim; applied to 295 real codes (`reports/final/partB/21`). |
| R-049 | 17/969 | Classify transitions: hard cut, jump cut, zoom cut, match cut, swipe, fade, overlay reveal, split-screen reveal, text punch-in, UI zoom, B-roll replacement | data | ◐ partial | Schema (`scenes.transition_in`) defines the full transition-type list; `reports/final/partB/24-transition-analysis.md` states real data only ever populates hard_cut/same_shot/unknown — no evidence yet for the granular subtypes. |
| R-050 | 17A/1008 | Support automated server execution model: video queuing, local processing, transcript analysis, frame analysis, statistical analysis, categorization, reference selection, script generation, card generation, frame planning, persistence, reporting | code | ✓ done | `docs/M2RADAR_CONTENT_ENGINE_ARCHITECTURE.md` §11 + `engine/watchdog.py`/`local_pipeline.py` cover queuing→media→transcript→frames→alignment→stats→categorization→persistence→reporting; the former scheduling gap for semantic ta-v1/fa-v1 analysis is now closed by new module `engine/analyze_pending.py` (scheduled semantic-analysis stage, unattended `claude -p` batches); card/script generation still runs session-driven, not cron-wired. |
| R-051 | 17B/1063 | Ensure video is "analysis-ready" when available: metadata, statistics, media, transcript, segments, frames, scenes, transcript analysis, frame analysis, categories, performance features | data | ◐ partial | `video_state.analysis_ready` implements the strict definition; `engine.corpus.tiers()` defines a second, looser "ANALYSIS_READY" (transcript+frames only, no LLM analyses required) — two competing definitions coexist, named explicitly in the architecture doc line 198. |
| R-052 | 17C/1105 | Create consolidated analytical feature object: content semantics (20+ fields), script construction (30+ fields), visual construction (20+ fields), performance (5+ fields) | data | ✓ done | `engine/features.py`: semantics block 20+ fields, script block 30+ fields, visual block 20+ fields, performance block 5+ fields — all field-counted directly from source. |
| R-053 | 18 | Analyze creator-level metrics: count, followers, median/mean/MAD/IQR views, coefficient of variation, median likes/comments/shares/saves, engagement, posting frequency, outlier share, consistency score | analysis | ✓ done | `engine/stats.py::creator_stats()`: count/followers/median/mean/MAD/IQR/SD/CV, median likes/comm/resh/save, engagement, posts/week, outlier_share, consistency_score; 132 creators (`reports/final/partB/11-creator-analysis.md`). |
| R-054 | 18 | Analyze video-level metrics: views, engagement metrics, duration, transcript length, hook length, words-per-second, topic, pain, solution, CTA, scene count, cut frequency, A-roll/B-roll ratio, relative performance | analysis | ✓ done | `video_features` + `engine/stats.py::video_perf()`: views/engagement/duration/transcript length/hook length/wps/topic/pain/solution/CTA/scenes/cuts/roll-ratio/relative performance. |
| R-055 | 18 | Use robust metrics: Performance Ratio = Views / Median Creator Views; Robust Z = (Views - Median) / (1.4826 × MAD); flag small samples | analysis | ◐ partial | `robust_z_log()` implements the 1.4826×MAD scaling in log-space (ln(1+views)) — a documented deliberate deviation from the spec's raw-views formula; "Performance Ratio" is implemented as a lift (value/reference − 1), same information under a different constant. |
| R-056 | 19 | Classify creators: consistent (high median, low dispersion, repeated strong performance) vs high-variance (large spread, few peaks) | analysis | ✓ done | `reports/final/partB/28-best-creators.md`: CONSISTENT tier (7 named creators, CV<1) vs HIGH_VARIANCE tier (11 named, e.g. CV 4.77). |
| R-057 | 19 | Select cross-creator high-performing video independently if clear outlier, analytically explainable, relevant, and structurally useful | analysis | ✓ done | `reports/final/partB/29-best-individual-reels.md`: 5 named cross-creator outlier reels selected with multi-criteria reasoning; one candidate explicitly excluded and reasoned. |
| R-058 | 20/1313 | Categorize every available reel: topic, subject, subtopic, audience, pain, desire, problem, solution, outcome, hook type, proof type, CTA type, narrative archetype, positioning type, funnel role | data | ◐ partial | `video_features` has 14 of 15 named dimensions as real columns, populated for 266 of 3,211 ingested reels (direct DB count); no `outcome` column (closest is `payoff` inside `raw_labels_json`); "every available reel" not met — only the 266/268 analysis-ready reels are categorized, not the full 3,211. |
| R-059 | 20/1313 | Use canonical categories; store raw inferred labels AND canonical normalized category | data | ✓ done | `engine/ingest_analysis.py` enforces canonical vocabulary via fixed HOOK_TYPES/PAINS/etc. sets + `_canon()`; raw labels preserved in `video_features.raw_labels_json` alongside canonical columns. |
| R-060 | 21 | Analyze: topic frequency vs performance, pain vs performance, solution type vs performance, hook type vs performance, hook duration vs performance, CTA type vs performance, transcript length vs performance, scene count vs performance, cut frequency vs performance, A-roll/B-roll ratio vs performance, frame type vs performance | analysis | ✓ done | `reports/data/associations.json` (100+ features tested) + `reports/data/category_freq_perf.json`; narrative coverage in `reports/final/partB/14-26`. |
| R-061 | 21 | Treat results as descriptive/associational; do not infer causality automatically | analysis | ✓ done | `reports/final/partB/26-performance-analysis.md` / `14-topic-analysis.md`: explicit "associational...nothing here is causal" language. |
| R-062 | 21A | Generate explicit insights with: statement, evidence, supporting videos/creators, relevant metrics/patterns, confidence, limitations, strategic implication | analysis | ✓ done | `data/analysis/insights.json` — exactly 30 entries (counted), each with statement/claim/evidence/supporting_codes/supporting_creators/confidence/limitations/implication. |
| R-063 | 21B | Generate multiple content hypotheses before selecting final ideas; each hypothesis includes: ID, concept, audience, fit, pain, outcome, hook, thesis, solution, proof, CTA, visual structure, insights, references, expected strengths, risks, novelty, confidence | analysis | ✓ done | `data/analysis/hypotheses.json` — exactly 24 entries (counted), each with the full required field set (audience/pain/outcome/hook/thesis/solution/proof/cta/visual_structure/insights/refs/strengths/risks/novelty/confidence). |
| R-064 | 21C | Evaluate hypotheses: evidence strength, reference quality, positioning relevance, audience relevance, novelty, clarity, hook potential, depth, visual potential, feasibility, transformation potential, risk of being generic | analysis | ✓ done | Each hypothesis carries a scores object + total_score; ranking verified clean: 10 SELECTED (min score 4.16) vs 14 REJECTED (max score 3.67), no overlap. |
| R-065 | 21C | Store hypothesis scores, reviewer comments, rejected/selected status | data | ✓ done | `status` field (SELECTED/REJECTED, 10/14 split) + `reviewer_comment` field present per hypothesis. |
| R-066 | 21D | Select references hypothesis-specific; define what video needs; search corpus for high-value; select for functional roles (topic, hook, pain framing, explanation, proof, CTA, A-roll, B-roll, split-screen, visual rhythm, editing) | analysis | ✓ done | `data/analysis/hypothesis_refs.json` — exactly 41 entries (counted), each with function/reason/useful_beat_ids/useful_scene_ids/performance. |
| R-067 | 21D | Document why each reference was selected; record exact influence; prefer small, explainable reference set | doc | ✓ done | `reason` field documents exact selection rationale per reference with quoted evidence; average 4.1 references per hypothesis (41/10). |
| R-068 | 22/2508 | Follow content generation chain: data → analysis → insight → hypothesis → ranking → hypothesis-specific references → transformation → script → frame plan → review (not direct analysis to script) | code | ✓ done | `engine/cards_pipeline.py::finalize()` implements the literal chain as a traceable job sequence (CARD_GENERATION→SCRIPT_GENERATION→SCRIPT_REVIEW→FRAME_PLAN→FRAME_GENERATION→VIDEO_GENERATION_READY); upstream data→insight→hypothesis→ranking→refs chain verified via the JSON files. |
| R-069 | 22/2508 | Combine elements from multiple reels: hook from A, pain from B, explanation from C, proof from D, CTA from E, A-roll from F, B-roll from G, editing rhythm from H | analysis | ✓ done | `hypothesis_refs.json[].function` shows a distinct source-per-functional-role pattern, e.g. H-01 draws explanation/pain/proof each from a different reel. |
| R-070 | 22/2508 | Final script must trace back: which reference influenced hook/thesis/structure/proof/visuals; what was deliberately changed; what is original | doc | ✓ done | Card `traceability` object (`cards/C-2026-09-12-01.json`): `evidence_summary`, `original_synthesis`, `why_better_than_generic`. |
| R-071 | 23/1562 | Record transformation types: adapt proven topic, reframe for audience, combine related reels, apply hook to different concept, use visual grammar with new content, transform AI/business idea to use-case, combine evidence into new POV | analysis | ◐ partial | `hypothesis_refs.json[].transformation` covers the 7 named transformation types as free-text prose ("Borrowed: X. Changed: Y."), not a closed enum/tagged field (grepped, none found). |
| R-072 | 23/1562 | Record which references influenced concept, what was borrowed structurally, what was transformed, what is original, why it fits positioning | doc | ✓ done | Same `transformation` field (borrowed/changed/original) plus card `traceability.why_better_than_generic` ties to positioning fit. |
| R-073 | 24/1586 | Use knowledge base for expertise (marketing principles, psychology, script rules, hook principles, storytelling, positioning) not as replacement for source evidence | code | ◐ partial | `engine/prompts/script-writer.md` + `script-reviewer.md` function as the knowledge base, explicitly subordinated to evidence ("no invented numbers"); no single consolidated "knowledge base" file/directory exists — distributed across POSITIONING.md/PRODUCTION.md/RULES.md/prompt files. |
| R-074 | 25 | Supply script writer with structured context package: audience, positioning, topic, selected reels, URLs, stats, reason for each reference, transcripts, features, patterns, frame structures, analytical conclusions, knowledge-base guidance | doc | ◐ partial | `engine/prompts/script-writer.md` names every required input and where to find it, functionally assembling the context package; no single generated "context package" artifact per hypothesis exists — assembled by the writer following prompt instructions across scattered files. |
| R-075 | 26 | Write original short-form script with required structure: hook, setup, problem/pain, explanation, solution, proof/example, payoff, CTA (where appropriate) | content | ✓ done | All 10 `cards/*.json` have `script.sections[]` with required roles; role coverage is validator-enforced (`cards_v2.validate()`). |
| R-076 | 26 | Capture in script: final text, section labels, word counts, estimated duration, total word count, target words-per-second, tone, intended emotional effect | content | ✓ done | Card schema: `script.sections[].{role,text,words,seconds}`, `total_words`, `total_s`, `target_wps`, `tone`, `emotional_effect` — present in every card file. |
| R-077 | 27/1681 | Make script frame-aware during writing: determine which lines should be A-roll/B-roll/split-screen/UI-proof/text-overlay; when visual changes/transitions happen; emphasize hook visually; how CTA appears visually | content | ✓ done | `storyboard[]` scenes carry `frame_type`, `layout` (a_roll/split_screen/demo/motion_graphic), `overlay_text`, `transition`; contiguous timing validated by `cards_v2.validate()`. |
| R-078 | 27/1681 | Co-develop script and frame plan; do not create separately | content | ✓ done | Single `script-writer.md` prompt requires frame decisions made "while writing each section", not a separate pass; script and storyboard live in one card JSON. |
| R-079 | 28/1702 | Review every generated script: check evidence grounding, source relevance, originality, hook strength, clarity, pacing, emotional flow, audience/positioning fit, spoken naturalness, density, CTA, visual compatibility, frame coherence | code | ✓ done | `engine/prompts/script-reviewer.md` 10-point checklist executed on all 10 cards (status:REVIEWED, 7-14 concrete findings each, verified via direct JSON dump). |
| R-080 | 28/1702 | Store both original and revised versions | data | ✓ done | DB table `script_versions` holds both versions per card (20 rows = 2×10); `review.revised=true` on all 10 card JSONs. |
| R-081 | 29 | Replicate functional information architecture of prior Notion cards: concept, rationale, source references, source performance, script, sections, visual scenes, frame images, editing instructions, reference rationale | content | ✓ done | `reports/data/existing_cards.json`: 23 legacy pre-v2 cards inventoried, informing the v2 schema's functional architecture. |
| R-082 | 30 | Create exactly 10 optimized final content cards based on integrated analysis | content | ✓ done | Exactly 10 files `cards/C-2026-09-12-01.json`…`-10.json` (counted, excludes the `-EX` example card). |
| R-083 | 30 | Each card includes: strategic layer (title, concept, audience, objective, positioning, problem, promise, reason), reference layer (reels, creator, URL, performance, ratio, reason), script layer (full text, sections, durations, word counts), visual layer (scenes, A-roll/B-roll, splits, transitions, proof, CTA), editing layer (timing, captions, emphasis, motion, assets) | content | ✓ done | `cards/README.md` schema confirms all 5 required layers present in every card: strategy/reference/script/visual/editing. |
| R-084 | 31 | Create time-aligned frame/scene table for each card covering full script: Time, Script, Part, Frame Type, Visual, Source Inspiration, Transition, Asset | content | ✓ done | Cardbook markdown per card (`reports/final/cardbook/01-...md`): exact Time | Script | Part | Frame Type | Visual | Source Inspiration | Transition | Asset table, contiguous over the full script. |
| R-085 | 32/2838 | Generate representative visual frame images for planned scenes (where infrastructure available) reflecting script meaning, positioning, analyzed structures, reference frames, scene type, hierarchy, A-roll/B-roll/split-screen intent, text overlay, CTA | content | ◐ partial | Real PNGs ARE generated per scene (`engine/storyboard_render.py`, `cards/frames/*.png`, 88 files total) reflecting scene type/layout/roll-intent/overlay/CTA, but every one is a Pillow-drawn placeholder panel (`asset_status:'template'` on all of them), not AI-generated imagery — image-gen providers `openai`/`higgsfield` both NOT_CONFIGURED; `cards/README.md` states outright "never a stock photo, never AI-generated imagery". The requirement's own "(where infrastructure available)" hedge is honestly satisfied by not fabricating a render rather than by producing one. |
| R-086 | 33/1860 | Ground frame generation in analysis: high-performing reference frames, recurring visual patterns, frame-to-transcript alignment, cut/visual-change frequency, split-screen usage, A-roll/B-roll balance, hook frame structure, transitions, CTA visualization | content | ✓ done | Storyboard scenes grounded via `source_inspiration` (reference reel + reason) per scene, hook-frame rule from insight I-09, PRODUCTION.md's cut rule encoded in prompt rule 5; same template-only caveat as R-085 applies to the rendered artifact itself. |
| R-087 | 33/1860 | Explain why each frame structure was selected | doc | ✓ done | Cardbook "Source inspiration" column carries an explicit reason per scene (e.g. "borrowed the habit of putting per-item numbers on screen rather than summarising them out loud"). |
| R-088 | 34/1879 | Transform generated visuals into user's positioning/brand: reuse composition logic, split-screen logic, pacing, rhythm, UI proof, A-roll/B-roll alternation, caption structure, transition style; make final output original | content | ✗ not done | No AI-generated visuals exist anywhere in the codebase to "transform" (image-gen providers unconfigured, `storyboard_render.py` explicitly Pillow-only) — the requirement's premise doesn't apply; composition/roll-alternation/pacing logic is instead baked directly into Pillow templates from brand tokens, a different mechanism than "transform generated visuals." |
| R-089 | 35 | Use videos without transcripts as topic/visual/performance/format references using available: caption, title, topic, subject, stats, frames, visual structure, hashtags, creator context | analysis | ✗ not done | Corpus-tier vocabulary can identify transcript-less reels (FRAMES_ONLY, 22-29 codes), but grep across `cards/`, `hypothesis_refs.json`, `hypotheses.json` finds zero references to any FRAMES_ONLY code used as a reference; `reports/final/partC/38-next-steps.md` lists ratifying these codes as an open decision for Misha, i.e. not yet actioned. |
| R-090 | 35 | Mark transcript-dependent features as unavailable; do not fabricate speech | data | ✓ done | `reports/data/flags.json`: MISSING_TRANSCRIPT flagged on 2,936 codes; no-fabrication rule explicit in `engine/edl.py` ("never invents a speech asset", `speech_policy: REQUIRED_RECORDED_SPEECH`). |
| R-091 | 36 | Analyze frames even without full media access where stored frames exist; reconstruct sequence, use for visual-pattern analysis; mark limitations explicitly | analysis | ✓ done | `engine/schema.py` corpus_tier has FRAMES_ONLY as a first-class value (tested); `reports/charts/visual_sequence_top10.json` runs frame-only pattern analysis; limitations documented (`docs/M2RADAR_ANALYSIS_METHOD.md` §1.1). |
| R-092 | 37 | Use Remotion as main programmatic composition/rendering system; support: scene configuration, frame timing, captions, assets, split-screen layouts, overlays, transitions, components, typography, audio, aspect ratio, render outputs | code | ◐ partial | `studio/remotion/src/Composition.tsx`+`types.ts` implement scene config/frame timing/captions/assets/split-screen/overlays/typography/audio/aspect ratio/render output, proven by one real executed render (60.05s, 1080x1920, h264, `reports/evidence/remotion-previs-*`); "transitions" are only a metadata label, never consumed by the renderer as a visual effect — no crossfade/wipe logic exists in `contract.mjs`/`Composition.tsx`. |
| R-093 | 37 | Make card's structured JSON/YAML usable by Remotion | data | ✓ done | `engine/edl.py::card_to_edl()` converts a validated card into an `m2.remotion-edl.v1` document, validated via `contract.mjs`; tested (`test_edl_partition_and_node_contract`) and exercised for real. JSON only, permitted by the requirement's "JSON/YAML" phrasing. |
| R-094 | 38 | Prepare OpenGenAI/external provider layer: adapters, externalized keys, no secrets in Git, missing keys handled clearly, state tracing records execution, frame prompts/asset specs persist | code | ◐ partial | Externalized keys confirmed gitignored; `engine/providers.py` gives clear CONFIGURED/NOT_CONFIGURED/DISABLED states per provider, tested (`tests/test_providers.py`); but no adapter code calls OpenAI/Higgsfield anywhere (grep-confirmed empty) and no per-scene frame-prompt/asset-spec is persisted — the config layer exists and is tested, the execution layer does not exist. |
| R-095 | 38 | Do not falsely claim generation succeeded | code | ✓ done | Render receipt status is `RENDERED_REVIEW_REQUIRED` (never "success"), `provider_execution:false` explicitly recorded; every storyboard frame stamped `asset_status:'template'`. |
| R-096 | 40 | Locate and inspect prior GPT-5.6 Luna run: cards, scripts, references, stats, transcripts, frames, trace, report, errors, quality; compare old vs new output; keep old artifacts | doc | ✓ done | `reports/audit/04-existing-cards-review.md` §D: thorough multi-branch/history/disk search concludes no Luna run ever existed (only a model-preference label, `model_api_enabled:false` in 17 places); old artifacts preserved, not deleted. |
| R-097 | 41 | Synthesize all existing content: what works/doesn't, which topics strongest, which pains recur, which hooks perform, which CTAs work, which creators consistent, which transcripts correlate with performance, which visuals correlate, A-roll/B-roll patterns, transition styles, frame-change frequency, script transformation approach, what to avoid | doc | ✓ done | `reports/analysis/01-general-conclusions.md` (343 lines, §A-H) + 02-10 cover the full required synthesis list. |
| R-098 | 42 | Create analytical graphs where data supports: creator medians/dispersion, video performance ratios, topic performance, pain performance, hook performance, script length vs performance, hook length vs performance, duration vs performance, CTA vs performance, scene count vs performance, cut frequency vs performance, A-roll/B-roll vs performance, split-screen vs performance | analysis | ✓ done | `reports/charts/index.json`: 18 charts covering all 13 required categories plus 4 extra. |
| R-099 | 42 | Every chart includes: title, labels, sample size, source, interpretation, limitations | doc | ✓ done | Programmatic check of all 18 chart JSONs confirms every one carries title/n (sample size)/source/interpretation/limitations. |
| R-100 | 43 | Create analytical PDF with 38 sections: Executive Summary, Architecture Review, Main vs `latest`/Max Fork Comparison, Database Reconciliation, Migration, State Tracing, Data Acquisition, Transcript Pipeline, Frame Pipeline, Corpus Coverage, Creator Analysis, Video Analysis, Transcript Categorization, Topic/Pain/Solution/Hook/CTA/Script Part/Frame Analysis, A-roll/B-roll/Split-Screen/Transition Analysis, Transcript-to-Frame Alignment, Performance Analysis, Reference Selection Logic, Best Creators/Reels, Conclusions, Recommendations, Script/Frame/Video Architecture, Limitations, Next Steps | doc | ✓ done | `reports/final/partA` (10 files) + `partB` (23) + `partC` (5) = exactly 38 numbered sections, compiled into `reports/final/M2RADAR_ANALYTICAL_REPORT.pdf` (132 pages, verified via PDF page count); section naming maps to but is not 1:1 with the spec's list — count and coverage correct, naming variance only. |
| R-101 | 43 | Include direct links to relevant reels in analytical PDF | doc | ✓ done | `reports/final/partB/29-best-individual-reels.md` contains 16 direct instagram.com/reel/ links; `28-best-creators.md` contains 2. |
| R-102 | 44 | Create card/storyboard PDF with 10 cards clearly separated; each includes: title, strategy, reference reels, performance evidence, full script, sections, timing, frame storyboard, generated images, editing instructions, A-roll/B-roll/split-screen labels, visual proof, CTA, asset list | doc | ◐ partial | Exactly 10 cards + intro = 11 files in `reports/final/cardbook/`, compiled into `M2RADAR_CARD_BOOK.pdf` (105 pages); sampled card has every listed element except real "generated images" — only Pillow template placeholders (`asset_status:'template'`), consistent with R-095's no-fabrication rule but not literal AI-generated images. |
| R-103 | 44 | Card PDF functions as production-ready creative brief | doc | ✓ done | Sampled card carries Claims (OBSERVED/PLANNED/TO_MEASURE/MISSING) with sources, Traceability, Review history, Production state (EDL path + Remotion validation) — functions as a genuine production-ready brief. |
| R-104 | 45 | Save every card in structured JSON/YAML format with scene objects: scene_id, start_time, end_time, script_text, script_role, frame_type, visual_description, transition, overlay_text, reference_video_ids, reference_frame_ids, asset_status, asset_path | data | ◐ partial | Scene keys present in substance (`scene_id`, `start_s`/`end_s`, `script_text`, `script_role`, `frame_type`, `visual`, `transition`, `overlay_text`, `source_inspiration`, `asset_status`, `asset_path`) but 5 of 13 field names differ from the spec's literal names (e.g. `start_s` vs `start_time`, `visual` vs `visual_description`, nested `source_inspiration` array vs separate `reference_video_ids`/`reference_frame_ids`). |
| R-105 | 46 | Create docs/M2RADAR_CONTENT_ENGINE_ARCHITECTURE.md | doc | ✓ done | `docs/M2RADAR_CONTENT_ENGINE_ARCHITECTURE.md` exists, 684 lines. |
| R-106 | 46 | Create docs/M2RADAR_DATABASE_RECONCILIATION.md | doc | ✓ done | `docs/M2RADAR_DATABASE_RECONCILIATION.md` exists, 451 lines. |
| R-107 | 46 | Create docs/M2RADAR_ANALYSIS_METHOD.md | doc | ✓ done | `docs/M2RADAR_ANALYSIS_METHOD.md` exists, 816 lines. |
| R-108 | 46 | Create reports/M2RADAR_INTEGRATION_EXECUTION_REPORT.md | doc | ✓ done | `reports/M2RADAR_INTEGRATION_EXECUTION_REPORT.md` now on disk (808 lines) with a §13 DoD self-check and §16 final execution summary; 4 fields are left as literal orchestrator placeholders (`<<COMMIT_HASH>>`, `<<LATEST_SYNC>>`, `<<NOTION_RESULT>>`, `<<SERVER_DEPLOY>>`) pending the orchestrator's post-delivery steps. |
| R-109 | 46 | Create analytical PDF and card/storyboard PDF | doc | ✓ done | Both `reports/final/M2RADAR_ANALYTICAL_REPORT.pdf` (132 pages) and `M2RADAR_CARD_BOOK.pdf` (105 pages) exist and open. |
| R-110 | 46 | Create structured card JSON/YAML/database records | data | ✓ done | 10 card JSON files in `cards/`; `engine/cards_v2.py::save()` persists to DB (tested: `test_save_idempotent_and_writes_db_rows`). |
| R-111 | 47 | Test branch integrity, `latest` preservation, clean migration path | test | ◐ partial | `tests/test_engine_schema.py::test_full_migration_validation_block` gives an automated migration-path test; "branch integrity"/"latest preservation" have no automated test — verified only manually via `git branch -a`. |
| R-112 | 47 | Test database: row counts, key relationships, no silent loss, transcript/frame/card links | test | ✓ done | `engine/migrate_legacy.py::_orphans()` checks exactly the required relationships, tested, cross-checked against real `reports/migration-validation.json` (`foreign_key_violations: []`, honest non-zero orphan counts reported rather than hidden). |
| R-113 | 47 | Test Hiker adapter loading and missing key handling | test | ✓ done | `tests/test_providers.py` (incl. `test_hiker_config_configured_from_env_never_prints_value`) + `tests/test_hiker_client.py` (incl. `test_key_never_appears_in_any_written_file_or_log`). |
| R-114 | 47 | Test transcription paths: Loore/Lure and local; normalized output | test | ◐ partial | Local transcription path implemented and tested; no Loore/Lure transcription call site exists anywhere in `engine/` (only config-presence tests in `test_providers.py`) — "normalized output" comparison between the two paths is untestable because one path doesn't exist as code, consistent with the local-first-by-design intent of R-028/030 but literally unmet as written. |
| R-115 | 47 | Test transcript analysis: all transcripts processed, categories stored, parts stored | test | ✓ done | New `tests/test_ingest_analysis.py` (5 tests: writes beats with correct fields, unknown-label logging, empty-file rejection, idempotent re-ingest) now directly tests the module that loads all-transcripts-processed / categories-stored / parts-stored, which previously had no dedicated coverage. |
| R-116 | 47 | Test frame analysis: frames processed, transcript alignment, scene labels | test | ✓ done | Frame extraction/alignment solidly tested (`test_local_pipeline.py`); scene-label ingestion (fa-v1→frame_labels) now covered by new `tests/test_ingest_analysis.py::test_ingest_frames_writes_frame_labels_and_legacy_scenes`. |
| R-117 | 47 | Test analytics: medians, dispersion, performance ratios, robust outliers | test | ✓ done | `tests/test_stats.py` (27 tests) covers medians/dispersion/performance ratios/robust-z/bootstrap CI extensively. |
| R-118 | 47 | Test reference selection, script writer (receives references/transcripts/stats/frames, generates original transformed script), reviewer (revises/stores versions), frame plan, frame generation with provider availability handling, card generation (exactly 10), Remotion input validation, state tracing end-to-end | test | ✓ done | End-to-end orchestration now covered by new `tests/test_cards_pipeline.py` (`test_finalize_valid_card_writes_jobs_frames_db_and_edl`, `test_finalize_invalid_card_raises_and_leaves_failed_job`) and new `tests/test_cards_inventory.py::test_exactly_ten_dated_cards`; EDL/Remotion input validated (`test_edl_partition_and_node_contract`); provider availability tested (`test_providers.py`). Remaining soft gap: "reference selection"/"script writer" are agent-authored analytical content (`reports/analysis/09-reference-selection.md`) without a corresponding tested Python selection function. |

## Phase 2: Watchdog for Newest Hiker Reels

| ID | Section | Requirement (one line, verbatim-close) | Type | Status | Evidence |
|----|---------|---------|------|--------|----------|
| R-119 | 60 | Distinguish current validated analytical corpus from newest Hiker-ingested reels not yet locally processed | data | ✓ done | `engine/corpus.py` + `reports/data/corpus_tiers.json`/`reports/final/partA/10-corpus-coverage.md`: ANALYSIS_READY(268)/FRAMES_ONLY(22)/TRANSCRIPT_UNUSABLE(7)/INGESTED_NOT_ANALYZED(2,914) of 3,211. |
| R-120 | 60 | Mark newest unprocessed reels with explicit state (INGESTED_NOT_ANALYZED, LOCAL_TRANSCRIPTION_PENDING, LOCAL_FRAME_EXTRACTION_PENDING, WATCHDOG_PENDING) | data | ◐ partial | `engine/state.py` grants equivalent granular per-code states (transcript_state, frames_state, corpus_tier incl. INGESTED_NOT_ANALYZED), but none of the 4 literal names in the requirement (e.g. LOCAL_TRANSCRIPTION_PENDING) exist verbatim (grep-confirmed) — functionally equivalent, not literally named. |
| R-121 | 60 | Exclude newest Hiker-only reels from high-confidence transcript conclusions, feature distributions, frame-pattern conclusions, reference selection | data | ✓ done | Every analysis chapter states its denominator explicitly and excludes the 2,914 INGESTED_NOT_ANALYZED codes from every conclusion. |
| R-122 | 60 | Always distinguish: N_ingested ≠ N_transcript-analyzed ≠ N_frame-analyzed ≠ N_fully-analysis-ready; never hide denominator differences | doc | ✓ done | `reports/data/corpus_tiers.json` + `10-corpus-coverage.md` name all four denominators (3,211 ≠ 275 ≠ 268 ≠ 297) every time, never hidden. |
| R-123 | 61 | Phase 1 must be completed, tested, committed, and pushed to main before Phase 2 begins | git | ◐ pending orchestrator | Phase 1 work is functionally complete and tested (342 passed / 1 skipped), but NOT committed and NOT pushed: `content-engine` and `main` are the identical commit `92e71e7a...` and `git status --porcelain` shows `engine/`, `reports/`, `cards/`, `tests/`, `docs/M2RADAR_*`, `schemas/`, `studio/`, `tools/`, `config/`, `reference/` all untracked (directly verified). Phase 2 itself has not begun either, so the ordering constraint isn't violated, but the commit/push gate is unmet — commit+push are the orchestrator's next step by design of this task. |
| R-124 | 92 | Watchdog finds records: ingested=true AND analysis_ready=false AND no_active_processing_job=true | code | ✓ done | `engine/watchdog.py::_needs_processing_codes` selects exactly `ingested=true AND analysis_ready=false AND no_active_processing_job=true`; tested (`test_watchdog_selects_only_not_ready_codes_with_live_urls`). |
| R-125 | 92 | Process each reel: media validation, local transcript, segmentation, local screenshot/keyframe, scene detection, transcript-scene alignment, semantic analysis, visual analysis, statistical normalization, category update, analysis-ready state | code | ✓ done | `engine/local_pipeline.py::process_video` runs media validation→transcription→frame/scene extraction→alignment→state refresh in one pipeline, tested; semantic/visual LLM analysis is a separate stage (`engine/ingest_analysis.py`, now tested — see R-115). |
| R-126 | 92 | Watchdog must be idempotent, retryable, observable, configurable, rate-limited, safe against duplicate workers, version-aware | code | ✓ done | Idempotent/retryable (tested), lock-protected against duplicate workers (`data/watchdog.lock`, `test_watchdog_lock_prevents_duplicate_worker`), rate-limited (`RATE_SLEEP_S=1.0`), configurable (`--limit`/`--codes`/`--dry`), version-aware, observable (runs/jobs tables + printed summary). |

## Phase 3: Production (MP4 Contract, Remotion, Supabase, Cloudflare)

| ID | Section | Requirement (one line, verbatim-close) | Type | Status | Evidence |
|----|---------|---------|------|--------|----------|
| R-127 | 82 | Define supplied MP4 shot contract: card ID, scene ID, script segment, take number, shot type, A-roll/B-roll role, duration, FPS, resolution, orientation, audio presence/quality, subject, sync notes, quality status, retake status | data | → phase 2/3 | Phase 3 (real MP4 production) deferred by design; the contract itself is already fully defined and matches the requirement almost field-for-field: `schemas/supplied-take.schema.json` = card_id/scene_id/script_segment/take/shot_type/roll/duration_s/fps/width/height/orientation/has_audio/audio_quality/subject/in_s/out_s/sync_notes/quality_status/retake_of/sha256/path. |
| R-128 | 82 | Accept and validate supplied MP4 shots; map to approved scenes; select takes; trim/arrange; add B-roll/screens; build splits; add captions/overlays; apply transitions; sync cuts to beats; render; QA; store | code | → phase 2/3 | Phase 3 deferred; groundwork exists and is tested (`engine/production.py`: `ingest_takes`/`validate_take`/`select_takes`/`edl_with_takes`/`render_plan`, 26 tests in `tests/test_production.py`), but no real owner footage has ever been ingested — zero actual supplied MP4s exist in the repo. |
| R-129 | 82 | Do not ignore the approved storyboard when processing MP4 shots | code | → phase 2/3 | Phase 3 deferred; by design `edl_with_takes()` only swaps `placeholder` layers for real `video` layers and keeps the rest of the approved EDL/storyboard untouched — the approved storyboard is never ignored, per its own docstring and tests. |
| R-130 | 83 | Prepare Supabase as storage layer for generated/rendered video artifacts; link to: run ID, card ID, script/storyboard/render version, source MP4, final path, thumbnail, duration, metadata, QA status, created_at | code | → phase 2/3 | Phase 3 deferred; `schemas/render-record.schema.json` already matches the requirement field-for-field: run_id/card_id/script_version/storyboard_version/render_version/source_take_ids/final_object_key/thumbnail_key/duration_s/render_metadata/qa_status/storage_provider/created_at. `engine/storage.py::SupabaseStorage` implements it but is NOT_CONFIGURED (no `SUPABASE_URL`/`SUPABASE_SERVICE_KEY`) — prepared, not connected. |
| R-131 | 83 | Use Supabase Storage or canonical Supabase convention; avoid multiple sources of truth | code | → phase 2/3 | Phase 3 deferred; `engine/storage.py` explicitly designates Supabase as the canonical store with local fallback only ("§84 explicitly forbids a second one" in its own docstring) — the no-multiple-sources-of-truth rule is encoded in code, not yet exercised live. |
| R-132 | 84 | Inspect existing Cloudflare code/plan; document intended role; prepare interfaces/configuration; keep secrets external; implement only justified roles (CDN, R2, Workers, routing, delivery, queues) | code | → phase 2/3 | Phase 3 deferred; `CloudflareDelivery` class implements delivery-only role (`public_url()` reads `CF_PUBLIC_BASE_URL`), documented in `docs/M2RADAR_CONTENT_ENGINE_ARCHITECTURE.md` §12.4 as the only justified role; state is NOT_CONFIGURED (stub, no credentials) — role defined and scoped, not connected. |
| R-133 | 84 | Do not create redundant canonical video store if Supabase is designated generated-video storage layer | code | → phase 2/3 | Phase 3 deferred; `CloudflareDelivery`'s own docstring states it "never uploads anything: Supabase is the canonical generated-video store" — redundant store explicitly avoided by design. |

## Analysis Requirements (Cross-Phase)

| ID | Section | Requirement (one line, verbatim-close) | Type | Status | Evidence |
|----|---------|---------|------|--------|----------|
| R-134 | 63 | Extract linguistic features where reliable: keywords, keyphrases, repeated phrases, n-grams, sentence openings/endings, parts of speech, pronouns, imperatives, modals, questions, negations, intensifiers, comparisons, superlatives, numbers, named entities, tools, models, platforms, technical terms, emotional words, fear/risk/opportunity/urgency/identity/novelty/evidence/CTA words | analysis | ✓ done | `engine/lexical.py` (603 lines) implements every listed feature family (keywords/keyphrases/n-grams/openings-endings/pronouns/imperatives/modals/questions/negations/intensifiers/comparisons/superlatives/numbers/entities/technical terms/emotional-fear-opportunity-urgency-identity-novelty-proof-CTA lexicons), each labelled heuristic vs exact; 23/23 tests pass. |
| R-135 | 63 | Calculate where meaningful: lexical diversity, average/median sentence length, phrase-length distribution, claims per 10s, proof per 10s, questions per 10s, direct-address frequency, numeric-evidence frequency | analysis | ✓ done | Same module: TTR/RTTR/MATTR50 (diversity), avg/median sentence length + distribution, claims/proof/questions-per-10s, direct-address and numeric-evidence rates — all present and tested. |
| R-136 | 63 | Finish each transcript with structured interpretation: Message, Intent, Audience tension, Offer/value, Mechanism, Emotion, Proof, Structural reason, Visual reason | analysis | ✓ done | Every `data/analysis/transcripts/*.json` carries all 9 required fields verbatim (message/intent/audience_tension/offer/mechanism/emotion/proof/structural_reason/visual_reason); beats loaded for 264/268 usable transcripts. |
| R-137 | 64/2923 | Reconstruct visual sequence completely using hierarchy: Video → Scene → Shot/Visual Beat → Representative Frame | analysis | ✓ done | `engine/scenes.py` + `engine/align.py` implement the Video→Scene→Frame hierarchy; `reports/final/partB/21-frame-analysis.md` documents it end to end. |
| R-138 | 64/2923 | Analyze frame/scene/cut metrics: first frame, hook frame, first 1/3/5s, scene count, cut count, cut frequency, first-cut timestamp, average/median scene length, A-roll/B-roll/split/screen/text/motion duration, CTA visual duration | analysis | ✓ done | `engine/align.py::_compute_features` computes first_cut_s/hook_cut_count/cta_cut_count/semantic_boundary_cut_rate/etc; `reports/final/partB/21,24` add scene/cut counts, cut frequency and roll-duration shares. |
| R-139 | 64/2923 | Classify scene types and transitions using controlled taxonomy; do not confuse sampled frames with real cuts | data | ✓ done | `reports/final/partB/24-transition-analysis.md` explicitly separates DB scene transitions (663) from raw fa-v1 sample-pair transitions (1,306) and states plainly that sampled frames ≠ real cuts. |
| R-140 | 65/3006 | Extract repeatable visual sequences (e.g., HOOK_CLOSEUP → SPLIT_SCREEN_PROOF → A_ROLL_EXPLANATION) and their performance | analysis | ✓ done | `reports/analysis/03-visual-patterns.md` "Core pattern" sections give sequence, frequency, creators, reels and performance context for each repeatable visual sequence. |
| R-141 | 65/3006 | For each sequence: where it occurs, what script part, what function, strong/weak performer appearance, positioning fit, influence on future storyboard | analysis | ✓ done | Same sections also give script-part occurrence, function, strong/weak-performer appearance, positioning fit and storyboard influence per pattern. |
| R-142 | 66/3034 | Align every video with both transcript and visual timing; for each beat: overlapping scenes, cut timing vs phrases, visual changes at semantic boundaries, visual proof/illustration/contrast/decoration, visual leads/lags speech, pattern-interrupt status | analysis | ✓ done | `engine/align.py` computes overlapping scenes / cut-timing-vs-phrases / visual-proof / leads-lags / pattern-interrupt; `reports/final/partB/25-transcript-to-frame-alignment.md` reports them with an explicit "measurement floor, not a finding" caveat. |
| R-143 | 66/3034 | Create alignment features: semantic-boundary cut rate, visual-proof alignment, hook/CTA alignment, claim-to-proof lag, visual change per sentence, scene changes per chapter (use only definable, validatable measures) | analysis | ✓ done | Same module computes exactly the 6 named alignment features (semantic-boundary cut rate, visual-proof alignment, hook/CTA alignment, claim-to-proof lag, visual change per sentence, scene changes per chapter). |
| R-144 | 69/3199 | Calculate engagement rate metrics: Like Rate, Comment Rate, Share Rate, Save Rate; protect against tiny denominators; where useful: comments/likes, shares/likes, saves/likes, (shares+saves)/views, high-intent engagement share | analysis | ✓ done | `engine/stats.py::video_perf` computes like/comment/share/save rate with a `MIN_PLAY_FOR_RATE` denominator guard; hi-intent-engagement rate present. |
| R-145 | 70/3236 | Calculate creator-relative lift: View Lift, Share Rate Lift; retain creator median, MAD, IQR, robust z-score, percentile rank, consistency score | analysis | ✓ done | `view_lift`/`share_rate_lift`/`save_rate_lift`/`robust_z`/`percentile_in_creator`/`creator_consistency` all computed and tested (`tests/test_stats.py`, 27 tests). |
| R-146 | 71/3276 | Analyze temporal change where chronology exists: views vs previous reel, engagement rate changes, share/save rate changes, rolling trend/dispersion, slope, log change; define every metric explicitly | analysis | ✓ done | `engine/stats.py::temporal` + `reports/data/temporal.json`: delta_play/delta_share_rate/delta_save_rate/rolling-median ratio/log_play_slope, each with an explicit definition string. |
| R-147 | 72/3307 | Use appropriate methods for cross-metric relationships: Spearman correlation, rank comparison, creator-normalized relationships, category medians, percentile comparisons, robust group differences; report n, definition, estimate, dispersion, direction, interpretation, limitation | analysis | ✓ done | `reports/final/partB/26-performance-analysis.md` reports Spearman rho/p/n/creator-normalised value/confidence tier for every pair (460 pairs tested, 13 RELIABLE). |
| R-148 | 73/3345 | Analyze strong/median/weak performers and consistent/inconsistent creators; find structural contrasts not only winners | analysis | ✓ done | `reports/final/partB/28-best-creators.md` + `reports/analysis/04-strong-vs-weak.md` analyze consistent-vs-high-variance creators and strong-vs-weak quartiles with structural, not just winner, contrasts. |
| R-149 | 74/3370 | Insights must combine quantitative and qualitative evidence: claim, sample size, metric, comparison, transcript evidence, frame evidence, interpretation, strategic implication, confidence, limitations (not vague claims like "Fast hooks perform better") | analysis | ✓ done | `data/analysis/insights.json`: all 30 entries carry statement/claim/metric/n/comparison/evidence/supporting_codes/supporting_creators/transcript_pattern/frame_pattern/interpretation/implication/confidence/limitations — matches the required field list exactly. |
| R-150 | 78/3495 | Infer with evidence what viewers appear to seek: desired outcomes, unresolved pains, tools/models/workflows, status gains, economic outcomes, time-savings, uncertainty reduction, proof needs, novelty/insider-information, reassurance, entertainment/curiosity; connect to topics/hooks/saves/comments/patterns; mark inference confidence | analysis | ✓ done | `reports/analysis/02-what-viewers-seek.md` covers desired outcomes/pains/tools with per-category n/lift/share/save and confidence labels, tied to topics/hooks/saves/comments. |
| R-151 | 80/3558 | Create dedicated section identifying core visual patterns: name, sequence, frequency, creators, reels, performance context, transcript context, viewer effect, reason it works, where/when to use in cards | analysis | ✓ done | `reports/analysis/03-visual-patterns.md` "Core pattern" sections match every required field (name/sequence/frequency/creators/reels/performance/transcript context/viewer effect/reason/where-when-to-use). |

## Notion Integration Requirements

| ID | Section | Requirement (one line, verbatim-close) | Type | Status | Evidence |
|----|---------|---------|------|--------|----------|
| R-152 | 85 | Upgrade Notion layer; use Content Engine Tool page (not M2-Signal-Studio) as authoritative operational dashboard | notion | ◐ partial | `engine/notion_sync.py` hard-codes the Content Engine Tool page as the dashboard target (M2 Signal+Studio explicitly demoted to a "Legacy" bookmark); plan generated but not yet applied live (`reports/notion-sync-plan.md`: mode DRY, no writes made). |
| R-153 | 86 | Notion main dashboard shows: Architecture (version, commit, pipeline, Hiker state, local transcription state, frame state, analysis state, hypothesis state, card state, production state) | notion | ◐ partial | `build_dashboard_blocks()` renders the Architecture section (version/commit/pipeline/per-provider state) verbatim per `reports/notion-sync-plan.md`; not yet applied to live Notion. |
| R-154 | 86 | Notion main dashboard shows: Data coverage (creators, ingested reels, reels with stats/transcripts/frames, fully analyzed, pending watchdog, cards, videos produced) | notion | ◐ partial | Same function's "Data coverage" section covers all 8 named metrics; not yet applied to live Notion. |
| R-155 | 86 | Notion main dashboard shows: Current analytics (best accounts/reels, creator medians/dispersion, engagement rates, topic/pain/hook/solution/CTA/structure/visual performance, scene/cut metrics) | notion | ◐ partial | "Current analytics" section exists but is thinner than the full requirement (top-accounts table only; topic/pain/hook/etc. performance lives in separate report chapters, not surfaced on the dashboard page itself); not yet applied. |
| R-156 | 86 | Notion main dashboard shows: Insights (statement, number, sample, supporting reels/accounts, transcript/visual pattern, implication, confidence) | notion | ◐ partial | "Insights" section renders each of the 30 insights with metric/n/confidence/pattern/implication, plus a planned dedicated Insights database; not yet applied. |
| R-157 | 86 | Notion main dashboard shows: Hypotheses (hypothesis, score, evidence, references, selected/rejected status, resulting card) | notion | ◐ partial | "Hypotheses" section + planned database (title/score/status/evidence/references/resulting card); not yet applied. |
| R-158 | 86 | Notion main dashboard shows: Cards (concept, status, script, reference reels, performance rationale, storyboard, generated frames, production state) | notion | ◐ partial | "Cards" section + planned Cards v2 database; not yet applied. |
| R-159 | 86 | Notion main dashboard shows: Runs (major execution run page links) | notion | ◐ partial | "Runs" section + planned Runs database link major runs; not yet applied. |
| R-160 | 87 | Each analyzed reel has detailed page/record: URL, account, date, raw/normalized stats, creator-relative lift, transcript chapters, hook/pain/solution/proof/CTA, timings, keyphrases, emotional analysis, intention, frame sequence, screenshots, A-roll/B-roll, scene/cut metrics, visual sequence, categories, reference score, reason selected/not-selected, cards using it | notion | ◐ partial | Planned "Reels analysis" database (297 rows in the plan) carries the required field list, tested (`tests/test_notion_sync.py`); not yet applied. |
| R-161 | 88 | Each creator/account has detailed page: metadata, followers, analyzed reel count, medians/means, dispersion, consistency score, engagement rates, top reels, outliers, topic/hook/pain/visual-pattern distribution, posting frequency, conclusions, reliability as reference | notion | ◐ partial | Planned "Accounts analysis" database covers most fields; posting-frequency/outliers/topic-distribution live in `reports/final/partB/28` rather than as confirmed literal Notion columns; not yet applied. |
| R-162 | 89 | Create category/infographic views for: topics, subtopics, pains, solutions, hooks, CTAs, narrative structures, emotions, tools/models, A-roll/B-roll, split-screen, visual sequences, frame/cut patterns; show frequency AND performance; use infographics where helpful | notion | ◐ partial | `categories_rows()` builds 6 of ~13 requested dimensions (topic/pain/hook/solution/cta/visual_sequence); subtopics/narrative-structures/emotions/tools-models/A-roll-B-roll/split-screen/frame-cut-patterns are not wired into the Categories sync as their own rows, though the underlying data exists in reports. |
| R-163 | 90 | Clean active Notion experience: archive/label stale legacy metrics, duplicates, obsolete runs, broken views, outdated snapshots, content not connected to current database (prefer archive over deletion) | notion | ◐ partial | `legacy_section()` labels (not deletes) the M2 Signal+Studio page and the two Signal one-off DBs (`reports/audit/05-notion-state.md` §6); "M2 Kanban" and 4 "North Hux" databases are flagged as out-of-scope/not investigated, not addressed by the sync plan. |
| R-164 | 91 | Each run page includes: run ID, date/time, Git commit, model/prompt versions, data scope, creator count, reel count, transcript count, frame count, fully analyzed count, Hiker status, local transcription status, local frame status, analysis status, issues, warnings, generated reports, cards, artifacts, next steps | notion | ◐ partial | `runs_rows()` produces the required field list (tested: `test_runs_rows_carries_derived_fields`) but "counts"/"statuses" are generic JSON blobs rather than separately named per-stage columns — schema coarser than spec'd; not yet applied. |

## Testing & Validation Requirements

| ID | Section | Requirement (one line, verbatim-close) | Type | Status | Evidence |
|----|---------|---------|------|--------|----------|
| R-165 | 93 | Test branch comparison, database migration, column mapping, row counts, orphan/duplicate detection | test | ✓ done | `tests/test_engine_schema.py`: migration-creation/idempotency/guarded-columns/legacy-data-kept/orphan-detection/duplicate-detection all pass; branch comparison is a one-time documented analysis (`reports/final/partA/03`), appropriately not a pytest target. |
| R-166 | 93 | Test Hiker normalization, secret externalization, Hiker credential handling | test | ✓ done | `tests/test_hiker_client.py` (13, incl. key-never-in-logs) + `tests/test_providers.py` (25) cover normalization/secret-externalization/credential-handling. |
| R-167 | 93 | Test local transcription path, local frame extraction, screenshot persistence, scene detection, transcript-scene alignment, transcript chapter segmentation | test | ✓ done | `tests/test_local_pipeline.py` covers local transcription/frame extraction/screenshot persistence/scene detection/alignment; the previously-missing `ingest_analysis.py` coverage is now closed by new `tests/test_ingest_analysis.py` (5 tests). |
| R-168 | 93 | Test lexical/semantic features, cut metrics, creator baselines, normalized engagement, lift metrics | test | ◐ partial | Lexical (23 tests) and creator-baseline/normalized-engagement/lift metrics (`tests/test_stats.py`, 27 tests) fully covered; `engine/features.py`'s cut-metric computation itself still has no dedicated test file (`test_features.py` does not exist). |
| R-169 | 93 | Test hypothesis generation, reference selection, script generation, card generation, Remotion input, Supabase contract, Cloudflare integration boundary, Notion sync, report calculations | test | ✓ done | Hypothesis/reference ingestion (`tests/test_ingest_insights.py`, 11), card generation (`test_cards_v2.py`/`test_cardbook.py`/new `test_cards_pipeline.py`/new `test_cards_inventory.py`), Remotion input (`test_edl_partition_and_node_contract`), Supabase/Cloudflare (`test_production.py`), Notion sync (`test_notion_sync.py`, 35), report calculations (`test_stats.py`) — all pass. |
| R-170 | 93 | Resolve fixable failures; do not finish with known fixable failures; document all changes | test | ✓ done | `python3 -m pytest -q` → 342 passed, 1 skipped, 0 failed (current run, up from 252 after 3 new test files were added). The one skip is an opt-in real-ASR test gated behind `M2_REAL_ASR=1`, not an unresolved failure. |
| R-171 | 48/2299 | Never fabricate: stats, transcript text, frame contents, links, provider runs, performance results, generation outputs, render outputs | code | ✓ done | No single "no-fabrication" policy document exists verbatim, but the principle is enforced pervasively in code (`engine/production.py`, `engine/migrate_legacy.py`, `engine/cards_v2.py`, `engine/notion_sync.py` all contain explicit "never fabricate" logic/comments) and consistently in every report read during verification. |
| R-172 | 48/2299 | When information missing: mark it, explain impact, continue with justified fallback; use confidence labels (Reliable, Probable, Insufficient evidence) | code | ✓ done | RELIABLE/PROBABLE/INSUFFICIENT is a formal enum (`engine/ingest_insights.py` `CONFIDENCE_LABELS`, `engine/schema.py`) used consistently across `insights.json`/`notion_sync.py`/every analysis chapter, always paired with an explicit limitation statement. |

## Final Reporting & Traceability

| ID | Section | Requirement (one line, verbatim-close) | Type | Status | Evidence |
|----|---------|---------|------|--------|----------|
| R-173 | 79/3529 | Use clear analytical paragraphs; every conclusion includes: numerical evidence, comparison baseline, transcript interpretation, visual interpretation, strategic implication | doc | ✓ done | `reports/final/partB/31-what-works.md` and similar chapters use narrative paragraphs combining numerical evidence, comparison baseline, transcript quotes, visual reading and strategic implication together (spot-checked chapters 30/31/executive-summary). |
| R-174 | 79/3529 | Every chart/table followed by interpretation; explain what works/doesn't, why, for whom, under which category, with confidence level | doc | ✓ done | Every chart is paired with a text chapter; tables carry an explicit Confidence column (RELIABLE/PROBABLE/INSUFFICIENT) and name the audience each finding applies to. |
| R-175 | 94/4084 | Final analytical report explicitly includes all 33 sections: architecture, merge, coverage, missingness, creators, reels, transcript structure, phrase/word, topic, pain, solution, hook, proof, CTA, emotion/intent, scene/frame, A-roll/B-roll, split-screen, cut frequency, visual sequences, normalized engagement, creator-relative lift, cross-metric, strong vs weak, insights, hypotheses, references, card logic, script logic, visual logic, limitations, next watchdog stage, Cloudflare/Supabase readiness | doc | ✓ done | 38 numbered sections (partA 10 + partB 23 + partC 5) cover all 33 named topics from the requirement; "proof" and "emotion/intent" are embedded within other chapters rather than getting a standalone file; total section count (38) doesn't literally match the requirement's "33" but content coverage is complete. |
| R-176 | 94/4084 | Every major conclusion supported by numbers | doc | ✓ done | Chapters cite p-values/rho/medians/n throughout, e.g. `partB/30`: "Of 115 numeric features tested, none separates ... at p<0.01." |
| R-177 | 95/4125 | Prove architectural traceability for every card: idea source, patterns, supporting reels, relative performance, transcript structure, visual structure, insights, hypothesis, references, transformation, script, storyboard, MP4 mapping, render, storage, traceability | doc | ◐ partial | Every card carries hypothesis_id/references/traceability/original_synthesis, and storyboard+EDL+validation exist for all 10; the render/storage leg of traceability is proven end-to-end for only 1 of 10 real cards (the example `C-2026-09-11-EX`) — expected, since no owner footage exists yet and Phase 3 hasn't started. |
| R-178 | 50/2392 | Final execution summary includes: repository, main branch, commit hash, `latest` intact, creators/videos/transcripts/frames/cards analyzed, new cards generated, frame plans/images generated, providers used/unavailable, tests passed/failed, migration result, document paths, strategic findings, limitations, blockers | doc | ✓ done | `reports/M2RADAR_INTEGRATION_EXECUTION_REPORT.md` §16 "Final execution summary" table covers every required field (repository/commit/latest/creators-videos-transcripts-frames-cards/new cards/frame plans/providers/tests/migration/document paths/findings/limitations); 4 fields are left as literal orchestrator placeholders (`<<COMMIT_HASH>>`, `<<LATEST_SYNC>>`, `<<NOTION_RESULT>>`, `<<SERVER_DEPLOY>>`) pending the orchestrator's post-delivery steps. |

## Phase Execution & Finalization

| ID | Section | Requirement (one line, verbatim-close) | Type | Status | Evidence |
|----|---------|---------|------|--------|----------|
| R-179 | 96/4146 | Follow authoritative Phase 1 execution order: inspect, inventory, reconcile, merge, audit, integrate Hiker, make local canonical, analyze all transcripts/frames, calculate features, analyze creators/videos, generate insights/hypotheses/references, write 10 scripts/storyboards, produce reports, update Notion, test, resolve, commit, push | code | ◐ partial | Execution order followed exactly through "produce reports" (inspect→inventory→reconcile→merge→audit→Hiker→local-canonical→analyze→features→insights/hypotheses/refs→ 10 scripts/storyboards→reports); "update Notion" is DRY-plan-only (zero live writes) and "commit, push" has not happened — both explicit, named gaps, the latter pending the orchestrator. |
| R-180 | 96/4146 | Follow authoritative Phase 2 watchdog order: deploy, find pending reels, process through local transcript-frame-analysis chain, update database/categories/Notion, mark analysis-ready | code | → phase 2/3 | `engine/watchdog.py` exists, is unit-tested, and is designed exactly to this order, but has not been run against the live corpus (1,312 of 1,535 newest codes still unprocessed); Phase 2 itself has not begun. |
| R-181 | 96/4146 | Follow authoritative Phase 3 production order: finalize MP4 contract, validate Remotion, prepare Supabase, finalize Cloudflare, ingest MP4s, map/cut/edit, render, QA, store, update trace/Notion | code | → phase 2/3 | Phase 3 explicitly deferred; real groundwork exists and is tested (`schemas/supplied-take.schema.json`, `engine/production.py`, `engine/storage.py`, `engine/edl.py`, `studio/remotion/contract.mjs`, one verified real PREVIS render), but no owner footage exists yet so ingest/map/cut/render/QA/store has not run for any real card. |
| R-182 | 97/4221 | Definition of Done: database merged, columns mapped/validated, no silent data loss, transcripts exhaustively analyzed, frames exhaustively analyzed, transcript features persisted, visual metrics measured, report distinguishes denominators, Hiker in Git without key, local transcription canonical, local frame processing canonical, Loore/Lure optional, engagement/lift/cross-metric metrics exist, insights numeric/grounded, hypotheses explicit, references hypothesis-specific, scripts original transformations, cards frame-aware, visual patterns identified | doc | ◐ partial | 17 of 18 named DoD facets are satisfied with cited evidence (migration, column mapping, no silent loss, denominators, Hiker-without-key, local-transcription/frame canonical, Loore optional, engagement/lift/cross-metric metrics, insights/hypotheses/references, frame-aware cards, visual patterns identified). The one explicit, named shortfall: "exhaustively analyzed" — only 268/3,211 (8.3%) transcripts+frames are analysis-ready, and it is a pre-selected top-ranked slice, not exhaustive or random, per the project's own limitations chapter. |
| R-183 | 97/4221 | Definition of Done: Notion Content Engine Tool current/navigable, legacy info cleaned/archived, reel/account/run/card subpages linked, MP4 parameters defined, Remotion/Supabase/Cloudflare roles defined, tests run, fixable issues resolved, reports numeric/argumented, all work committed/pushed before Phase 2 | doc | ◐ partial | Reports/tests/MP4-contract/Remotion-role/Supabase-Cloudflare-role facets are done; Notion sync is DRY-only (zero live writes, cleanup/linking planned not executed); "all work committed/pushed before Phase 2" is not done — nothing is committed, pending the orchestrator. |
| R-184 | 2/49 | Before final push inspect: branch, remote, diff, migrations, secrets, tests, generated reports, generated cards, generated frame assets | git | ◐ pending orchestrator | Every named inspection item is itself verifiably complete and cited (branch/remote/diff/migrations/secrets/tests/generated reports/cards/frame assets — see `reports/M2RADAR_INTEGRATION_EXECUTION_REPORT.md` §2,§12 and this pass's own direct `git status`/`git diff` checks); only the push action itself remains, which is the orchestrator's next step by design of this task. |
| R-185 | 51/2431 | Definition of Done (comprehensive): main, `latest`, Max's fork inspected and intact; all main data integrated; database columns explicitly mapped; all transcripts reviewed/categorized; topic/pain/solution/hook/CTA/part-lengths stored; frequency analysis performed; all frame data reviewed; frames aligned to segments; visual structures deeply analyzed; A-roll/B-roll/split/transitions categorized; every card/script evaluated; Luna run evaluated; conclusions explain what works/doesn't; statistics guide reference selection; different treatment of consistent vs high-variance creators; best videos selectable independently; scripts synthesize multiple references; scripts transform for positioning; scripts grounded in reels/transcripts/topics/stats; knowledge base provides expertise not replacement; scripts frame-aware; 10 optimized cards created; every card includes full script/storyboard/generated frames where possible; frames reflect analyzed grammar and positioning; card PDF production-ready; Remotion/video generation prepared; secrets not committed; state tracing end-to-end works; Hiker integration deployable without embedding key; server automation continues; local transcription default; local frame extraction default; Loore/Lure optional/non-blocking; each new/incomplete video processed through local transcript+frame; consolidated per-video analytics persisted; insights explicitly generated; multiple hypotheses generated/ranked; references hypothesis-specific; final scripts traceable to strongest transcripts/stats/frames; reports saved; code validated; implementation pushed to main | doc | ◐ partial | The large majority of named sub-clauses are satisfied with direct evidence: branch inspection/intactness, data integration, column mapping, in-scope transcript/frame categorization, frequency analysis, visual-structure categorization, 33 legacy cards + the (nonexistent) Luna run evaluated, statistics-guided reference selection, consistent-vs-high-variance treatment, independent best-video selection, multi-reference script synthesis/transformation, frame-aware cards, 10 cards with script+storyboard+template frames, card PDF, Remotion prepared, secrets not committed, state-tracing schema, Hiker deployable without the key, local-first defaults, per-video analytics persisted, insights/hypotheses/references generated. Explicit, named gaps: "all transcripts reviewed" is 8.3% top-ranked, not exhaustive; frame images are template placeholders for 10/10 cards but a real render is proven for only 1/10; "server automation continues" has no Phase 3 cron step; the Notion dashboard update is plan-only; "implementation pushed to main" has not happened (pending the orchestrator). |

---

## Required Output Paths

The specification names the following explicit file paths that must be created:

- **`docs/M2RADAR_CONTENT_ENGINE_ARCHITECTURE.md`** — Architecture documentation
- **`docs/M2RADAR_DATABASE_RECONCILIATION.md`** — Database mapping and migration details
- **`docs/M2RADAR_ANALYSIS_METHOD.md`** — Analytical methodology
- **`reports/M2RADAR_INTEGRATION_EXECUTION_REPORT.md`** — Execution report
- **Analytical PDF** — Polished 38-section report with charts, conclusions, and recommendations
- **Card/Storyboard PDF** — 10 production-ready cards with scripts, storyboards, generated frames
- **Structured Card JSON/YAML** — Machine-readable card data (scenes, timing, references, assets)
- **Notion `Content Engine Tool` page** — Authoritative operational dashboard with current architecture, data coverage, analytics, insights, hypotheses, cards, and run history

## Final Execution Summary Fields (Section 50)

The final summary must report:

- Repository location and main branch state
- Pushed commit hash
- Confirmation `latest` remains intact
- Total creators analyzed
- Total videos analyzed
- Total transcripts analyzed and coverage percentage
- Total frames analyzed and coverage percentage
- Cards reviewed and scripts reviewed
- Luna outputs reviewed
- New cards generated (target: 10)
- New scripts generated
- New frame plans generated
- Generated frame images count
- Providers used and providers configured but unavailable
- Tests passed and failed
- Database migration result
- Path to architecture document
- Path to database mapping document
- Path to analytical method document
- Path to execution report
- Path to analytical PDF
- Path to card PDF
- Path to structured cards
- Main strategic findings (3–5 key insights)
- Limitations and unresolved technical blockers

---

## Totals

185 rows scored (R-001–R-185). 128 ✓ done, 44 ◐ partial, 2 ◐ pending orchestrator, 9 → phase 2/3, 2 ✗ not done = 185.

| Status | Count |
|---|---:|
| ✓ done | 128 |
| ◐ partial | 44 |
| ◐ pending orchestrator | 2 |
| → phase 2/3 | 9 |
| ✗ not done | 2 |
| **Total** | **185** |

## Rows not marked ✓ done — with reason

One line each, grouped by status. Full evidence for each row is in the table above; this is the short "why not done" pointer.


**✗ not done** (2)

- **R-088** — No AI-generated visuals exist anywhere in the codebase to "transform" (image-gen providers unconfigured, `storyboard_render.py` explicitly Pillow-only) — the requirement's premise doesn't apply; composition/roll-alternation/pacing logic is instead baked directly into Pillow templates from brand tokens, a different mechanism than "transform generated visuals."
- **R-089** — Corpus-tier vocabulary can identify transcript-less reels (FRAMES_ONLY, 22-29 codes), but grep across `cards/`, `hypothesis_refs.json`, `hypotheses.json` finds zero references to any FRAMES_ONLY code used as a reference; `reports/final/partC/38-next-steps.md` lists ratifying these codes as an open decision for Misha, i.e. not yet actioned.

**◐ partial** (44)

- **R-006** — Cards/scripts/reports/Notion/Hiker/Loore/Remotion inspected with citations (`reports/audit/03,04,05`); "Luna" searched exhaustively and confirmed nonexistent (`reports/audit/04-existing-cards-review.md` §D). "OpenGenAI" never appears anywhere in the repo outside this checklist itself — flag as an unverifiable/nonexistent inspection target, not a missed inspection.
- **R-014** — `beats` table = 21 columns counted (exceeds the numeric 20+ bar), but topic/subject/pain/hook/solution/proof/CTA/transition are collapsed into a single `role` enum rather than separate columns — numeric threshold met, literal field-by-field match is not.
- **R-015** — `frame_labels` = 21 cols + `scenes` = 13 cols counted directly; speaker/face/UI/text-presence flags present, but no dedicated `product_present` column (would live inside `labels_json`).
- **R-021** — `engine/state.py` `STAGES` = 25 states, not 40 (`reports/final/partA/06-state-tracing.md`: "trimmed from his 41-stage DAG ... to the 25 stages this branch actually runs"); `JOB_STATES` lifecycle (PENDING/RUNNING/DONE/FAILED/RETRY_REQUIRED/SKIPPED) implemented and unit-tested.
- **R-026** — `lib/hiker.py::row()` normalizes into canonical fields; `fetch_log` table exists for raw-payload provenance (`engine/schema.py`) but holds 0 rows — no collection run has executed against this schema version yet.
- **R-031** — 266/268 usable transcripts analysed (99.25%), but the underlying 268-code corpus is itself only 8.3% of 3,211 ingested codes and is explicitly the top-ranked slice of `score.py`'s own ranking, not a random/complete set (`reports/final/partB/13-transcript-categorization.md`; execution report §14 item 1).
- **R-037** — `docs/M2RADAR_CONTENT_ENGINE_ARCHITECTURE.md` lines 394-401 map all 8 conceptual roles to concrete modules/prompts; only 4 actual prompt files exist (`engine/prompts/`) — several roles are folded into one ta-v1 prompt's structured output rather than separate agents.
- **R-040** — `engine/scenes.py` implements real scene-boundary detection, tested; per `reports/final/partA/09-frame-pipeline.md` no code in the DB yet carries `frames_version='scene-v1'` — all existing frame data is the legacy fixed-9-sample method.
- **R-042** — Scene-change detection, transcript-segment boundaries and keyframe extraction present; no OCR anywhere in the repo (grep-confirmed), no distinct "perceptual difference" method beyond the scene-score threshold, no evidenced semantic-grouping step.
- **R-046** — First-frame/hook-frame/scene-count/cut-frequency/roll-ratio/split-screen/text-overlay/framing/face-presence all covered (`reports/final/partB/21-24`); "body_movement" and "visual_metaphor" not found as named fields anywhere.
- **R-049** — Schema (`scenes.transition_in`) defines the full transition-type list; `reports/final/partB/24-transition-analysis.md` states real data only ever populates hard_cut/same_shot/unknown — no evidence yet for the granular subtypes.
- **R-051** — `video_state.analysis_ready` implements the strict definition; `engine.corpus.tiers()` defines a second, looser "ANALYSIS_READY" (transcript+frames only, no LLM analyses required) — two competing definitions coexist, named explicitly in the architecture doc line 198.
- **R-055** — `robust_z_log()` implements the 1.4826×MAD scaling in log-space (ln(1+views)) — a documented deliberate deviation from the spec's raw-views formula; "Performance Ratio" is implemented as a lift (value/reference − 1), same information under a different constant.
- **R-058** — `video_features` has 14 of 15 named dimensions as real columns, populated for 266 of 3,211 ingested reels (direct DB count); no `outcome` column (closest is `payoff` inside `raw_labels_json`); "every available reel" not met — only the 266/268 analysis-ready reels are categorized, not the full 3,211.
- **R-071** — `hypothesis_refs.json[].transformation` covers the 7 named transformation types as free-text prose ("Borrowed: X. Changed: Y."), not a closed enum/tagged field (grepped, none found).
- **R-073** — `engine/prompts/script-writer.md` + `script-reviewer.md` function as the knowledge base, explicitly subordinated to evidence ("no invented numbers"); no single consolidated "knowledge base" file/directory exists — distributed across POSITIONING.md/PRODUCTION.md/RULES.md/prompt files.
- **R-074** — `engine/prompts/script-writer.md` names every required input and where to find it, functionally assembling the context package; no single generated "context package" artifact per hypothesis exists — assembled by the writer following prompt instructions across scattered files.
- **R-085** — Real PNGs ARE generated per scene (`engine/storyboard_render.py`, `cards/frames/*.png`, 88 files total) reflecting scene type/layout/roll-intent/overlay/CTA, but every one is a Pillow-drawn placeholder panel (`asset_status:'template'` on all of them), not AI-generated imagery — image-gen providers `openai`/`higgsfield` both NOT_CONFIGURED; `cards/README.md` states outright "never a stock photo, never AI-generated imagery". The requirement's own "(where infrastructure available)" hedge is honestly satisfied by not fabricating a render rather than by producing one.
- **R-092** — `studio/remotion/src/Composition.tsx`+`types.ts` implement scene config/frame timing/captions/assets/split-screen/overlays/typography/audio/aspect ratio/render output, proven by one real executed render (60.05s, 1080x1920, h264, `reports/evidence/remotion-previs-*`); "transitions" are only a metadata label, never consumed by the renderer as a visual effect — no crossfade/wipe logic exists in `contract.mjs`/`Composition.tsx`.
- **R-094** — Externalized keys confirmed gitignored; `engine/providers.py` gives clear CONFIGURED/NOT_CONFIGURED/DISABLED states per provider, tested (`tests/test_providers.py`); but no adapter code calls OpenAI/Higgsfield anywhere (grep-confirmed empty) and no per-scene frame-prompt/asset-spec is persisted — the config layer exists and is tested, the execution layer does not exist.
- **R-102** — Exactly 10 cards + intro = 11 files in `reports/final/cardbook/`, compiled into `M2RADAR_CARD_BOOK.pdf` (105 pages); sampled card has every listed element except real "generated images" — only Pillow template placeholders (`asset_status:'template'`), consistent with R-095's no-fabrication rule but not literal AI-generated images.
- **R-104** — Scene keys present in substance (`scene_id`, `start_s`/`end_s`, `script_text`, `script_role`, `frame_type`, `visual`, `transition`, `overlay_text`, `source_inspiration`, `asset_status`, `asset_path`) but 5 of 13 field names differ from the spec's literal names (e.g. `start_s` vs `start_time`, `visual` vs `visual_description`, nested `source_inspiration` array vs separate `reference_video_ids`/`reference_frame_ids`).
- **R-111** — `tests/test_engine_schema.py::test_full_migration_validation_block` gives an automated migration-path test; "branch integrity"/"latest preservation" have no automated test — verified only manually via `git branch -a`.
- **R-114** — Local transcription path implemented and tested; no Loore/Lure transcription call site exists anywhere in `engine/` (only config-presence tests in `test_providers.py`) — "normalized output" comparison between the two paths is untestable because one path doesn't exist as code, consistent with the local-first-by-design intent of R-028/030 but literally unmet as written.
- **R-120** — `engine/state.py` grants equivalent granular per-code states (transcript_state, frames_state, corpus_tier incl. INGESTED_NOT_ANALYZED), but none of the 4 literal names in the requirement (e.g. LOCAL_TRANSCRIPTION_PENDING) exist verbatim (grep-confirmed) — functionally equivalent, not literally named.
- **R-152** — `engine/notion_sync.py` hard-codes the Content Engine Tool page as the dashboard target (M2 Signal+Studio explicitly demoted to a "Legacy" bookmark); plan generated but not yet applied live (`reports/notion-sync-plan.md`: mode DRY, no writes made).
- **R-153** — `build_dashboard_blocks()` renders the Architecture section (version/commit/pipeline/per-provider state) verbatim per `reports/notion-sync-plan.md`; not yet applied to live Notion.
- **R-154** — Same function's "Data coverage" section covers all 8 named metrics; not yet applied to live Notion.
- **R-155** — "Current analytics" section exists but is thinner than the full requirement (top-accounts table only; topic/pain/hook/etc. performance lives in separate report chapters, not surfaced on the dashboard page itself); not yet applied.
- **R-156** — "Insights" section renders each of the 30 insights with metric/n/confidence/pattern/implication, plus a planned dedicated Insights database; not yet applied.
- **R-157** — "Hypotheses" section + planned database (title/score/status/evidence/references/resulting card); not yet applied.
- **R-158** — "Cards" section + planned Cards v2 database; not yet applied.
- **R-159** — "Runs" section + planned Runs database link major runs; not yet applied.
- **R-160** — Planned "Reels analysis" database (297 rows in the plan) carries the required field list, tested (`tests/test_notion_sync.py`); not yet applied.
- **R-161** — Planned "Accounts analysis" database covers most fields; posting-frequency/outliers/topic-distribution live in `reports/final/partB/28` rather than as confirmed literal Notion columns; not yet applied.
- **R-162** — `categories_rows()` builds 6 of ~13 requested dimensions (topic/pain/hook/solution/cta/visual_sequence); subtopics/narrative-structures/emotions/tools-models/A-roll-B-roll/split-screen/frame-cut-patterns are not wired into the Categories sync as their own rows, though the underlying data exists in reports.
- **R-163** — `legacy_section()` labels (not deletes) the M2 Signal+Studio page and the two Signal one-off DBs (`reports/audit/05-notion-state.md` §6); "M2 Kanban" and 4 "North Hux" databases are flagged as out-of-scope/not investigated, not addressed by the sync plan.
- **R-164** — `runs_rows()` produces the required field list (tested: `test_runs_rows_carries_derived_fields`) but "counts"/"statuses" are generic JSON blobs rather than separately named per-stage columns — schema coarser than spec'd; not yet applied.
- **R-168** — Lexical (23 tests) and creator-baseline/normalized-engagement/lift metrics (`tests/test_stats.py`, 27 tests) fully covered; `engine/features.py`'s cut-metric computation itself still has no dedicated test file (`test_features.py` does not exist).
- **R-177** — Every card carries hypothesis_id/references/traceability/original_synthesis, and storyboard+EDL+validation exist for all 10; the render/storage leg of traceability is proven end-to-end for only 1 of 10 real cards (the example `C-2026-09-11-EX`) — expected, since no owner footage exists yet and Phase 3 hasn't started.
- **R-179** — Execution order followed exactly through "produce reports" (inspect→inventory→reconcile→merge→audit→Hiker→local-canonical→analyze→features→insights/hypotheses/refs→ 10 scripts/storyboards→reports); "update Notion" is DRY-plan-only (zero live writes) and "commit, push" has not happened — both explicit, named gaps, the latter pending the orchestrator.
- **R-182** — 17 of 18 named DoD facets are satisfied with cited evidence (migration, column mapping, no silent loss, denominators, Hiker-without-key, local-transcription/frame canonical, Loore optional, engagement/lift/cross-metric metrics, insights/hypotheses/references, frame-aware cards, visual patterns identified). The one explicit, named shortfall: "exhaustively analyzed" — only 268/3,211 (8.3%) transcripts+frames are analysis-ready, and it is a pre-selected top-ranked slice, not exhaustive or random, per the project's own limitations chapter.
- **R-183** — Reports/tests/MP4-contract/Remotion-role/Supabase-Cloudflare-role facets are done; Notion sync is DRY-only (zero live writes, cleanup/linking planned not executed); "all work committed/pushed before Phase 2" is not done — nothing is committed, pending the orchestrator.
- **R-185** — The large majority of named sub-clauses are satisfied with direct evidence: branch inspection/intactness, data integration, column mapping, in-scope transcript/frame categorization, frequency analysis, visual-structure categorization, 33 legacy cards + the (nonexistent) Luna run evaluated, statistics-guided reference selection, consistent-vs-high-variance treatment, independent best-video selection, multi-reference script synthesis/transformation, frame-aware cards, 10 cards with script+storyboard+template frames, card PDF, Remotion prepared, secrets not committed, state-tracing schema, Hiker deployable without the key, local-first defaults, per-video analytics persisted, insights/hypotheses/references generated. Explicit, named gaps: "all transcripts reviewed" is 8.3% top-ranked, not exhaustive; frame images are template placeholders for 10/10 cards but a real render is proven for only 1/10; "server automation continues" has no Phase 3 cron step; the Notion dashboard update is plan-only; "implementation pushed to main" has not happened (pending the orchestrator).

**◐ pending orchestrator** (2)

- **R-123** — Phase 1 work is functionally complete and tested (342 passed / 1 skipped), but NOT committed and NOT pushed: `content-engine` and `main` are the identical commit `92e71e7a...` and `git status --porcelain` shows `engine/`, `reports/`, `cards/`, `tests/`, `docs/M2RADAR_*`, `schemas/`, `studio/`, `tools/`, `config/`, `reference/` all untracked (directly verified). Phase 2 itself has not begun either, so the ordering constraint isn't violated, but the commit/push gate is unmet — commit+push are the orchestrator's next step by design of this task.
- **R-184** — Every named inspection item is itself verifiably complete and cited (branch/remote/diff/migrations/secrets/tests/generated reports/cards/frame assets — see `reports/M2RADAR_INTEGRATION_EXECUTION_REPORT.md` §2,§12 and this pass's own direct `git status`/`git diff` checks); only the push action itself remains, which is the orchestrator's next step by design of this task.

**→ phase 2/3** (9)

- **R-127** — Phase 3 (real MP4 production) deferred by design; the contract itself is already fully defined and matches the requirement almost field-for-field: `schemas/supplied-take.schema.json` = card_id/scene_id/script_segment/take/shot_type/roll/duration_s/fps/width/height/orientation/has_audio/audio_quality/subject/in_s/out_s/sync_notes/quality_status/retake_of/sha256/path.
- **R-128** — Phase 3 deferred; groundwork exists and is tested (`engine/production.py`: `ingest_takes`/`validate_take`/`select_takes`/`edl_with_takes`/`render_plan`, 26 tests in `tests/test_production.py`), but no real owner footage has ever been ingested — zero actual supplied MP4s exist in the repo.
- **R-129** — Phase 3 deferred; by design `edl_with_takes()` only swaps `placeholder` layers for real `video` layers and keeps the rest of the approved EDL/storyboard untouched — the approved storyboard is never ignored, per its own docstring and tests.
- **R-130** — Phase 3 deferred; `schemas/render-record.schema.json` already matches the requirement field-for-field: run_id/card_id/script_version/storyboard_version/render_version/source_take_ids/final_object_key/thumbnail_key/duration_s/render_metadata/qa_status/storage_provider/created_at. `engine/storage.py::SupabaseStorage` implements it but is NOT_CONFIGURED (no `SUPABASE_URL`/`SUPABASE_SERVICE_KEY`) — prepared, not connected.
- **R-131** — Phase 3 deferred; `engine/storage.py` explicitly designates Supabase as the canonical store with local fallback only ("§84 explicitly forbids a second one" in its own docstring) — the no-multiple-sources-of-truth rule is encoded in code, not yet exercised live.
- **R-132** — Phase 3 deferred; `CloudflareDelivery` class implements delivery-only role (`public_url()` reads `CF_PUBLIC_BASE_URL`), documented in `docs/M2RADAR_CONTENT_ENGINE_ARCHITECTURE.md` §12.4 as the only justified role; state is NOT_CONFIGURED (stub, no credentials) — role defined and scoped, not connected.
- **R-133** — Phase 3 deferred; `CloudflareDelivery`'s own docstring states it "never uploads anything: Supabase is the canonical generated-video store" — redundant store explicitly avoided by design.
- **R-180** — `engine/watchdog.py` exists, is unit-tested, and is designed exactly to this order, but has not been run against the live corpus (1,312 of 1,535 newest codes still unprocessed); Phase 2 itself has not begun.
- **R-181** — Phase 3 explicitly deferred; real groundwork exists and is tested (`schemas/supplied-take.schema.json`, `engine/production.py`, `engine/storage.py`, `engine/edl.py`, `studio/remotion/contract.mjs`, one verified real PREVIS render), but no owner footage exists yet so ingest/map/cut/render/QA/store has not run for any real card.
