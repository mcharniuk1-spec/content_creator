# M2 validation delivery — 5 September 2026

The Signal and Studio runtime is accepted with limitations. Full-corpus media analysis and a usable free generation backend remain blocked; this is not full MVP acceptance.

## Added capability

- A portable, read-only transcript benchmark with identity/hash bijection checks, account-cluster uncertainty, grouped validation, training-only category vocabularies and explicit missingness. Live p/q are suppressed. See m2-transcript-benchmark.md.
- A separate SQLite diagnostic export with snapshot hashes, actual stage states, event validation, artifact rechecks, metric units/denominators and issue resolution evidence. See m2-stage-diagnostics.md.
- A 65-field dictionary, including corrected Components semantics: eligible, supported robust-z scores for log1p(Views) and four action rates; View index is not a component.
- Reviewed reusable validation lessons and a separate exact-byte promotion receipt. The source note retains its candidate YAML as the reviewed snapshot; the promotion receipt establishes accepted reuse.

## Verification and interpretation

Independent Luna High review passed all 28 added tests (18 benchmark and 10 diagnostic cases), with no failures or skips. See m2-v2-review-receipt.json. The earlier maker dictionary receipt had stale hashes; the final independent receipt binds the corrected dictionary and the Notion revision-3 readback confirms delivery. This resolves that specific receipt limitation without changing the original review history.

The existing corpus has 2,352 Reel identities and 100 accounts. Only 89 of 91 transcript records bear words (3.784% corpus coverage). The primary text/outcome comparison has 73 complete rows. Its grouped model differences remain exploratory, not a winner or causal script rule. Missing source media means no verified frames or semantic scenes. Full data, the benchmark, selected views, eight Proposed original cards, field definitions and stage trace are delivered in the authorized private Notion report; private corpus data and credentials are excluded from this repository.

The existing run records 54 verified events, 25 completed stages with their actual limitations and 14 unstarted stages. Diagnostic reporting does not unlock gates. Frozen runs retain their pinned runtime; new code or inputs require a new run. The partner can replay existing exports using the operator guide, or deploy their separately authorized collector with environment-injected HikerAPI credentials. This local execution did not call HikerAPI or activate the server.

## Device and restart boundary

Heavy local ML is disabled. Four free local generative experiments failed visual quality; a fifth was interrupted after resource contention. Remotion composition proof is separate from video-model quality. Use one lightweight local process at a time; a future media worker needs measured memory, timeout and cleanup limits on a suitable host.

To complete the requested corpus run, supply the existing approved media cache/map. Then acquire/decode, transcribe, independently review timed text and scenes, recompute evidence-backed analysis and revisit drafts. Studio resumes after owner card/footage decisions. Paid generation requires the owner's provider setup. No payment, publishing or deployment was performed.
