# Story Framework Report — ST-023

## Truth state

- Storyboard: `ORIGINAL_LOCAL_PREVISUALIZATION`
- External provider: `NOT_RUN`
- Source media/frames: `NOT_COLLECTED`
- Real-person likeness: `NOT_USED`

## Use

`hook_broll` · `object_metaphor` · 9:16 · 6s. Use as a fast insert around a speaking person, not as a final publishable claim.

## Narrative contract

Protagonist: a safety reviewer. Objective: explain why access and approval differ. Obstacle: the key exists without permission to turn it. Stakes: a capability could be mistaken for authority. Payoff: the door opens only when both states align.

## Engagement logic

This package uses the family-specific path `hook → orientation → mechanism → obstacle → proof → handback`. The persistent target remains visible from orientation through action, proof/limitation, and payoff. Each operation changes a declared entity state; the final frame closes or bounds the opening question and hands back to A-roll.

## Clean-room and production limits

All characters, words, props, sequencing, and SVGs are original abstractions. Do not map them to a reference creator's identity, composition, voice, music, wording, or branded UI. Replace SVG plates only after a separate approved provider/footage/rights run. Keep final text, UI, charts, and captions editable.
